[package]
name = "xdp2-fuzz"
description = "XDP2 adversarial testing infrastructure — oracle, packet builders, PCAP mutation"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
xdp2-core = { path = "../xdp2-core" }
xdp2-protocols = { path = "../xdp2-protocols" }
xdp2-bench = { path = "../xdp2-bench" }

[dev-dependencies]
proptest = "1"
