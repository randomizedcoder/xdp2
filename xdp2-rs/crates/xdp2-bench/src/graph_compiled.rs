//! Auto-generated monomorphic parser: bench_parser
//!
//! Generated by xdp2-compiler from Parser IR.
//! Do not edit — regenerate from the IR source.

use xdp2_core::ParseError;

/// Maximum parse depth (prevents infinite recursion in cyclic graphs).
const MAX_DEPTH: u32 = 8;

/// Parse one packet through the generated graph.
#[inline]
pub fn parse_packet(pkt: &[u8]) -> Result<(), ParseError> {
    parse_ethernet(pkt, 0)
}

fn parse_ethernet(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 14 { return Err(ParseError::Length); }
    let hlen = 14;
    let next = u16::from_be_bytes([pkt[12], pkt[13]]) as i64;
    let rest = &pkt[hlen..];
    match next {
        0x0806 => parse_arp(rest),
        0x88a8 => parse_qinq(rest, depth + 1),
        0x8100 => parse_vlan(rest, depth + 1),
        0x86dd => parse_ipv6(rest, depth + 1),
        0x0800 => parse_ipv4(rest, depth + 1),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_vlan(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 4 { return Err(ParseError::Length); }
    let hlen = 4;
    let next = u16::from_be_bytes([pkt[2], pkt[3]]) as i64;
    let rest = &pkt[hlen..];
    match next {
        0x0806 => parse_arp(rest),
        0x88a8 => parse_qinq(rest, depth + 1),
        0x8100 => parse_vlan(rest, depth + 1),
        0x86dd => parse_ipv6(rest, depth + 1),
        0x0800 => parse_ipv4(rest, depth + 1),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_qinq(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 4 { return Err(ParseError::Length); }
    let hlen = 4;
    let next = u16::from_be_bytes([pkt[2], pkt[3]]) as i64;
    let rest = &pkt[hlen..];
    match next {
        0x0806 => parse_arp(rest),
        0x88a8 => parse_qinq(rest, depth + 1),
        0x8100 => parse_vlan(rest, depth + 1),
        0x86dd => parse_ipv6(rest, depth + 1),
        0x0800 => parse_ipv4(rest, depth + 1),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_ipv4(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 20 { return Err(ParseError::Length); }
    // Variable-length header
    let hlen = (pkt[0] as usize & 15) * 4;
    if hlen < 20 || hlen > pkt.len() { return Err(ParseError::Length); }
    let next = pkt[9] as i64;
    let rest = &pkt[hlen..];
    match next {
        51 => parse_ah(rest, depth + 1),
        4 => parse_ip_in_ip(rest, depth + 1),
        132 => parse_sctp(rest),
        1 => parse_icmpv4(rest),
        17 => parse_udp(rest),
        6 => parse_tcp(rest),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_ipv6(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 40 { return Err(ParseError::Length); }
    let hlen = 40;
    let next = pkt[6] as i64;
    let rest = &pkt[hlen..];
    match next {
        51 => parse_ah(rest, depth + 1),
        44 => parse_ipv6_frag(rest, depth + 1),
        60 => parse_ipv6_eh(rest, depth + 1),
        0 => parse_ipv6_eh(rest, depth + 1),
        132 => parse_sctp(rest),
        58 => parse_icmpv6(rest),
        17 => parse_udp(rest),
        6 => parse_tcp(rest),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_ipv6_eh(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 2 { return Err(ParseError::Length); }
    // Variable-length header
    let hlen = pkt[1] as usize * 8 + 8;
    if hlen < 2 || hlen > pkt.len() { return Err(ParseError::Length); }
    let next = pkt[0] as i64;
    let rest = &pkt[hlen..];
    match next {
        51 => parse_ah(rest, depth + 1),
        44 => parse_ipv6_frag(rest, depth + 1),
        60 => parse_ipv6_eh(rest, depth + 1),
        0 => parse_ipv6_eh(rest, depth + 1),
        132 => parse_sctp(rest),
        58 => parse_icmpv6(rest),
        17 => parse_udp(rest),
        6 => parse_tcp(rest),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_ipv6_frag(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 8 { return Err(ParseError::Length); }
    let hlen = 8;
    let next = pkt[0] as i64;
    let rest = &pkt[hlen..];
    match next {
        51 => parse_ah(rest, depth + 1),
        44 => parse_ipv6_frag(rest, depth + 1),
        60 => parse_ipv6_eh(rest, depth + 1),
        0 => parse_ipv6_eh(rest, depth + 1),
        132 => parse_sctp(rest),
        58 => parse_icmpv6(rest),
        17 => parse_udp(rest),
        6 => parse_tcp(rest),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_ip_in_ip(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 20 { return Err(ParseError::Length); }
    // Variable-length header
    let hlen = (pkt[0] as usize & 15) * 4;
    if hlen < 20 || hlen > pkt.len() { return Err(ParseError::Length); }
    let next = pkt[9] as i64;
    let rest = &pkt[hlen..];
    match next {
        1 => parse_icmpv4(rest),
        17 => parse_udp(rest),
        6 => parse_tcp(rest),
        _ => Err(ParseError::UnknownProto),
    }
}

fn parse_ah(pkt: &[u8], depth: u32) -> Result<(), ParseError> {
    if depth >= MAX_DEPTH { return Err(ParseError::MaxNodes); }
    if pkt.len() < 12 { return Err(ParseError::Length); }
    // Variable-length header
    let hlen = pkt[1] as usize * 4 + 8;
    if hlen < 12 || hlen > pkt.len() { return Err(ParseError::Length); }
    let next = pkt[0] as i64;
    let rest = &pkt[hlen..];
    match next {
        51 => parse_ah(rest, depth + 1),
        4 => parse_ip_in_ip(rest, depth + 1),
        132 => parse_sctp(rest),
        1 => parse_icmpv4(rest),
        17 => parse_udp(rest),
        6 => parse_tcp(rest),
        _ => Err(ParseError::UnknownProto),
    }
}

#[inline]
fn parse_tcp(pkt: &[u8]) -> Result<(), ParseError> {
    if pkt.len() < 20 { return Err(ParseError::Length); }
    Ok(())
}

#[inline]
fn parse_udp(pkt: &[u8]) -> Result<(), ParseError> {
    if pkt.len() < 8 { return Err(ParseError::Length); }
    Ok(())
}

#[inline]
fn parse_icmpv4(pkt: &[u8]) -> Result<(), ParseError> {
    if pkt.len() < 8 { return Err(ParseError::Length); }
    Ok(())
}

#[inline]
fn parse_icmpv6(pkt: &[u8]) -> Result<(), ParseError> {
    if pkt.len() < 8 { return Err(ParseError::Length); }
    Ok(())
}

#[inline]
fn parse_sctp(pkt: &[u8]) -> Result<(), ParseError> {
    if pkt.len() < 12 { return Err(ParseError::Length); }
    Ok(())
}

#[inline]
fn parse_arp(pkt: &[u8]) -> Result<(), ParseError> {
    if pkt.len() < 28 { return Err(ParseError::Length); }
    Ok(())
}

