[package]
name = "xdp2-protocols"
description = "XDP2 protocol definitions — Ethernet, IP, TCP, UDP, tunnels, and more"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
xdp2-core = { path = "../xdp2-core" }
zerocopy = { workspace = true }

[dev-dependencies]
