--- /dev/null
+++ b/pcap/proto_audit/llap.h
@@ -0,0 +1,17 @@
+/*
+ * llap.h — llap_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_LLAP_H
+#define PROTO_AUDIT_LLAP_H
+
+#include <stdint.h>
+
+/* Llap header — 3 bytes minimum */
+struct llap_header {
+    uint8_t  llap_dst;
+    uint8_t  llap_src;
+    uint8_t  llap_type;
+};
+
+#endif /* PROTO_AUDIT_LLAP_H */
