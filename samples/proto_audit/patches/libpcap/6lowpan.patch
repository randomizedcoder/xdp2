--- /dev/null
+++ b/pcap/proto_audit/6lowpan.h
@@ -0,0 +1,20 @@
+/*
+ * 6lowpan.h — 6lowpan_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_6LOWPAN_H
+#define PROTO_AUDIT_6LOWPAN_H
+
+#include <stdint.h>
+
+/* 6lowpan header — 287 bytes minimum */
+struct 6lowpan_header {
+    uint8_t  _pad0[8];
+    uint8_t  6lowpan_ecn;
+    uint8_t  _pad9[13];
+    uint8_t  6lowpan_ecn;
+    uint8_t  _pad23[2];
+    uint8_t  6lowpan_next;
+};
+
+#endif /* PROTO_AUDIT_6LOWPAN_H */
