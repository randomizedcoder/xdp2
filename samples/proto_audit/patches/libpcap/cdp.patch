--- /dev/null
+++ b/pcap/proto_audit/cdp.h
@@ -0,0 +1,17 @@
+/*
+ * cdp.h — cdp_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_CDP_H
+#define PROTO_AUDIT_CDP_H
+
+#include <stdint.h>
+
+/* CDP header — 378 bytes minimum */
+struct cdp_header {
+    uint8_t  cdp_version;
+    uint8_t  cdp_ttl;
+    uint16_t cdp_checksum;
+};
+
+#endif /* PROTO_AUDIT_CDP_H */
