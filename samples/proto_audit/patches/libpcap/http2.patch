--- /dev/null
+++ b/pcap/proto_audit/http2.h
@@ -0,0 +1,28 @@
+/* SPDX-License-Identifier: BSD-2-Clause-FreeBSD
+*
+* Auto-generated by proto-audit from IR definition.
+* Protocol: HTTP2
+*/
+
+#ifndef __XDP2_PROTO_HTTP2_H__
+#define __XDP2_PROTO_HTTP2_H__
+
+#include <linux/types.h>
+
+#include "xdp2/parser.h"
+
+#endif /* __XDP2_PROTO_HTTP2_H__ */
+
+#ifdef XDP2_DEFINE_PARSE_NODE
+
+/* xdp2_parse_http2 protocol definition
+*
+* Parse HTTP2 header
+ */
+static const struct xdp2_proto_def xdp2_parse_http2 __unused() = {
+	.name = "HTTP2",
+	.min_len = sizeof(struct unknown_hdr),
+	.ops.len = http2_length,
+};
+
+#endif /* XDP2_DEFINE_PARSE_NODE */
