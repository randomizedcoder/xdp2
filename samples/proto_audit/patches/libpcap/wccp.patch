--- /dev/null
+++ b/pcap/proto_audit/wccp.h
@@ -0,0 +1,17 @@
+/*
+ * wccp.h — wccp_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_WCCP_H
+#define PROTO_AUDIT_WCCP_H
+
+#include <stdint.h>
+
+/* Wccp header — 108 bytes minimum */
+struct wccp_header {
+    uint32_t wccp_message;
+    uint16_t wccp_message_header_version;
+    uint16_t wccp_message_header_length;
+};
+
+#endif /* PROTO_AUDIT_WCCP_H */
