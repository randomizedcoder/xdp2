--- /dev/null
+++ b/pcap/proto_audit/ethernet.h
@@ -0,0 +1,17 @@
+/*
+ * ethernet.h — ethernet_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_ETHERNET_H
+#define PROTO_AUDIT_ETHERNET_H
+
+#include <stdint.h>
+
+/* Ethernet header — 14 bytes minimum */
+struct ethernet_header {
+    uint8_t  ethernet_dst;
+    uint8_t  ethernet_src;
+    uint16_t ethernet_type;
+};
+
+#endif /* PROTO_AUDIT_ETHERNET_H */
