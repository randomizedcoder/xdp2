--- /dev/null
+++ b/pcap/proto_audit/ldp.h
@@ -0,0 +1,18 @@
+/*
+ * ldp.h — ldp_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_LDP_H
+#define PROTO_AUDIT_LDP_H
+
+#include <stdint.h>
+
+/* LDP header — 446 bytes minimum */
+struct ldp_header {
+    uint16_t ldp_version;
+    uint16_t ldp_pdu_len;
+    uint32_t ldp_lsr;
+    uint16_t ldp_lsid;
+};
+
+#endif /* PROTO_AUDIT_LDP_H */
