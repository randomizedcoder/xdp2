--- /dev/null
+++ b/pcap/proto_audit/bicc.h
@@ -0,0 +1,20 @@
+/*
+ * bicc.h — bicc_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_BICC_H
+#define PROTO_AUDIT_BICC_H
+
+#include <stdint.h>
+
+/* Bicc header — 245 bytes minimum */
+struct bicc_header {
+    uint32_t bicc_cic;
+    uint8_t  bicc_message_type;
+    uint8_t  _pad5[6];
+    uint8_t  bicc_optional_parameter_part_pointer;
+    uint8_t  _pad12[232];
+    uint8_t  bicc_parameter_type;
+};
+
+#endif /* PROTO_AUDIT_BICC_H */
