--- /dev/null
+++ b/pcap/proto_audit/sip.h
@@ -0,0 +1,18 @@
+/*
+ * sip.h — sip_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_SIP_H
+#define PROTO_AUDIT_SIP_H
+
+#include <stdint.h>
+
+/* SIP header — 458 bytes minimum */
+struct sip_header {
+    uint8_t  sip_request_line[38];
+    uint8_t  _pad38[2];
+    uint8_t  sip_msg_hdr[295];
+    uint8_t  sip_msg_body[123];
+};
+
+#endif /* PROTO_AUDIT_SIP_H */
