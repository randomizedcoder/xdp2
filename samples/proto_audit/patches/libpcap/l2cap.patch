--- /dev/null
+++ b/pcap/proto_audit/l2cap.h
@@ -0,0 +1,28 @@
+/* SPDX-License-Identifier: BSD-2-Clause-FreeBSD
+*
+* Auto-generated by proto-audit from IR definition.
+* Protocol: L2CAP
+*/
+
+#ifndef __XDP2_PROTO_L2CAP_H__
+#define __XDP2_PROTO_L2CAP_H__
+
+#include <net/bluetooth/l2cap.h>
+
+#include "xdp2/parser.h"
+
+#endif /* __XDP2_PROTO_L2CAP_H__ */
+
+#ifdef XDP2_DEFINE_PARSE_NODE
+
+/* xdp2_parse_l2cap protocol definition
+*
+* Parse L2CAP header
+ */
+static const struct xdp2_proto_def xdp2_parse_l2cap __unused() = {
+	.name = "L2CAP",
+	.min_len = sizeof(struct l2cap_hdr),
+	.ops.len = l2cap_length,
+};
+
+#endif /* XDP2_DEFINE_PARSE_NODE */
