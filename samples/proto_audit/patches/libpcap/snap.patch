--- /dev/null
+++ b/pcap/proto_audit/snap.h
@@ -0,0 +1,19 @@
+/*
+ * snap.h — snap_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_SNAP_H
+#define PROTO_AUDIT_SNAP_H
+
+#include <stdint.h>
+
+/* SNAP header — 8 bytes minimum */
+struct snap_header {
+    uint8_t  snap_dsap;
+    uint8_t  snap_ssap;
+    uint8_t  snap_control;
+    uint8_t  snap_oui;
+    uint16_t snap_cisco_pid;
+};
+
+#endif /* PROTO_AUDIT_SNAP_H */
