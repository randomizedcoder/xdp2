--- /dev/null
+++ b/pcap/proto_audit/ppi.h
@@ -0,0 +1,18 @@
+/*
+ * ppi.h — ppi_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_PPI_H
+#define PROTO_AUDIT_PPI_H
+
+#include <stdint.h>
+
+/* Ppi header — 84 bytes minimum */
+struct ppi_header {
+    uint8_t  ppi_version;
+    uint8_t  ppi_flags;
+    uint16_t ppi_length;
+    uint32_t ppi_dlt;
+};
+
+#endif /* PROTO_AUDIT_PPI_H */
