--- /dev/null
+++ b/pcap/proto_audit/bgp.h
@@ -0,0 +1,17 @@
+/*
+ * bgp.h — bgp_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_BGP_H
+#define PROTO_AUDIT_BGP_H
+
+#include <stdint.h>
+
+/* BGP header — 19 bytes minimum */
+struct bgp_header {
+    uint8_t  bgp_marker[16];
+    uint16_t bgp_length;
+    uint8_t  bgp_type;
+};
+
+#endif /* PROTO_AUDIT_BGP_H */
