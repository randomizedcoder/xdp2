--- /dev/null
+++ b/pcap/proto_audit/mysql.h
@@ -0,0 +1,17 @@
+/*
+ * mysql.h — mysql_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_MYSQL_H
+#define PROTO_AUDIT_MYSQL_H
+
+#include <stdint.h>
+
+/* Mysql header — 103 bytes minimum */
+struct mysql_header {
+    uint8_t  mysql_packet_length;
+    uint8_t  mysql_packet_number;
+    uint8_t  mysql_server_greeting[99];
+};
+
+#endif /* PROTO_AUDIT_MYSQL_H */
