--- /dev/null
+++ b/pcap/proto_audit/netmon_filter.h
@@ -0,0 +1,20 @@
+/*
+ * netmon_filter.h — netmon_filter_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_NETMON_FILTER_H
+#define PROTO_AUDIT_NETMON_FILTER_H
+
+#include <stdint.h>
+
+/* Netmon_filter header — 136 bytes minimum */
+struct netmon_filter_header {
+    uint16_t netmon_filter_version;
+    uint32_t netmon_filter_type;
+    uint32_t netmon_filter_app_major_version;
+    uint32_t netmon_filter_app_minor_version;
+    uint8_t  netmon_filter_app_name[112];
+    uint8_t  netmon_filter_filter[10];
+};
+
+#endif /* PROTO_AUDIT_NETMON_FILTER_H */
