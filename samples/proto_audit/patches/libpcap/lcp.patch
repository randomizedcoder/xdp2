--- /dev/null
+++ b/pcap/proto_audit/lcp.h
@@ -0,0 +1,17 @@
+/*
+ * lcp.h — lcp_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_LCP_H
+#define PROTO_AUDIT_LCP_H
+
+#include <stdint.h>
+
+/* Lcp header — 10 bytes minimum */
+struct lcp_header {
+    uint8_t  lcp_code;
+    uint8_t  lcp_identifier;
+    uint16_t lcp_length;
+};
+
+#endif /* PROTO_AUDIT_LCP_H */
