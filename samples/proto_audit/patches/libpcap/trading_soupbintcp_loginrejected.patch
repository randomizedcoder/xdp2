--- /dev/null
+++ b/pcap/proto_audit/soupbintcp_loginrejected.h
@@ -0,0 +1,15 @@
+/*
+ * soupbintcp_loginrejected.h — soupbintcp_loginrejected_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_SOUPBINTCP_LOGINREJECTED_H
+#define PROTO_AUDIT_SOUPBINTCP_LOGINREJECTED_H
+
+#include <stdint.h>
+
+/* SoupBinTCP_LoginRejected header — 1 bytes minimum */
+struct soupbintcp_loginrejected_header {
+    uint8_t  soupbintcp_loginrejected_rejectreasoncode;
+};
+
+#endif /* PROTO_AUDIT_SOUPBINTCP_LOGINREJECTED_H */
