--- /dev/null
+++ b/pcap/proto_audit/eap.h
@@ -0,0 +1,18 @@
+/*
+ * eap.h — eap_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_EAP_H
+#define PROTO_AUDIT_EAP_H
+
+#include <stdint.h>
+
+/* EAP header — 5 bytes minimum */
+struct eap_header {
+    uint8_t  eap_code;
+    uint8_t  eap_id;
+    uint16_t eap_len;
+    uint8_t  eap_type;
+};
+
+#endif /* PROTO_AUDIT_EAP_H */
