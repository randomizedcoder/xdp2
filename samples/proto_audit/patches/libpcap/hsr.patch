--- /dev/null
+++ b/pcap/proto_audit/hsr.h
@@ -0,0 +1,18 @@
+/*
+ * hsr.h — hsr_header definition for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_HSR_H
+#define PROTO_AUDIT_HSR_H
+
+#include <stdint.h>
+
+/* HSR tag (IEC 62439-3) — 6 bytes */
+struct hsr_header {
+    uint16_t hsr_path:4;
+    uint16_t hsr_lsdu_size:12;
+    uint16_t hsr_seq_nr;
+    uint16_t hsr_ether_type;
+};
+
+#endif /* PROTO_AUDIT_HSR_H */
