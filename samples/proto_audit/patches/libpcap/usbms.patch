--- /dev/null
+++ b/pcap/proto_audit/usbms.h
@@ -0,0 +1,20 @@
+/*
+ * usbms.h — usbms_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_USBMS_H
+#define PROTO_AUDIT_USBMS_H
+
+#include <stdint.h>
+
+/* Usbms header — 31 bytes minimum */
+struct usbms_header {
+    uint32_t usbms_dcbwsignature;
+    uint32_t usbms_dcbwtag;
+    uint32_t usbms_dcbwdatatransferlength;
+    uint8_t  usbms_dcbwflags;
+    uint8_t  usbms_dcbwtarget;
+    uint8_t  usbms_dcbwcblength;
+};
+
+#endif /* PROTO_AUDIT_USBMS_H */
