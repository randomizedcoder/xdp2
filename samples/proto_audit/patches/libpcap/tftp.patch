--- /dev/null
+++ b/pcap/proto_audit/tftp.h
@@ -0,0 +1,17 @@
+/*
+ * tftp.h — tftp_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_TFTP_H
+#define PROTO_AUDIT_TFTP_H
+
+#include <stdint.h>
+
+/* TFTP header — 20 bytes minimum */
+struct tftp_header {
+    uint16_t tftp_opcode;
+    uint8_t  tftp_source_file[12];
+    uint8_t  tftp_type;
+};
+
+#endif /* PROTO_AUDIT_TFTP_H */
