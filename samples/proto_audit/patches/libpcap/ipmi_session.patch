--- /dev/null
+++ b/pcap/proto_audit/ipmi_session.h
@@ -0,0 +1,18 @@
+/*
+ * ipmi_session.h — ipmi_session_header for proto-audit comparison.
+ */
+
+#ifndef PROTO_AUDIT_IPMI_SESSION_H
+#define PROTO_AUDIT_IPMI_SESSION_H
+
+#include <stdint.h>
+
+/* Ipmi_session header — 19 bytes minimum */
+struct ipmi_session_header {
+    uint8_t  ipmi_session_authtype;
+    uint32_t ipmi_session_sequence;
+    uint32_t ipmi_session_id;
+    uint8_t  ipmi_session_len;
+};
+
+#endif /* PROTO_AUDIT_IPMI_SESSION_H */
