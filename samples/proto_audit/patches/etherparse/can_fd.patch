--- /dev/null
+++ b/src/proto_audit/can_fd.rs
@@ -0,0 +1,17 @@
+//! CAN FD frame header (socketcan) — 72 bytes.
+
+/// CAN FD frame header (socketcan) — 72 bytes.
+pub struct CanFdHeader {
+    /// CAN ID + flags.
+    pub can_id: u32,
+    /// Data length.
+    pub len: u8,
+    /// Flags (BRS, ESI).
+    pub flags: u8,
+    /// Reserved.
+    pub reserved: u8,
+    /// Length to DLC.
+    pub len8_dlc: u8,
+    /// Data payload (up to 64 bytes).
+    pub data: [u8; 64],
+}
