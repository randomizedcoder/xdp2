--- /dev/null
+++ b/src/proto_audit/mpls.rs
@@ -0,0 +1,13 @@
+//! MPLS label stack entry (RFC 3032) — 4 bytes.
+
+/// MPLS label stack entry (RFC 3032) — 4 bytes.
+pub struct MplsHeader {
+    /// Label value (20 bits).
+    pub label: Bits20,
+    /// Traffic Class (formerly Experimental).
+    pub traffic_class: Bits3,
+    /// Bottom of Stack.
+    pub bottom_of_stack: bool,
+    /// Time to Live.
+    pub ttl: u8,
+}
