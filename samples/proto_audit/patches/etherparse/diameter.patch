--- /dev/null
+++ b/src/proto_audit/diameter.rs
@@ -0,0 +1,35 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: DiamG
+// Minimum header: 24 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct DiamGHeader {
+    pub version: u8,
+    pub drlen: u32,
+    pub drflags: u8,
+    pub drcode: u32,
+    pub drappid: u32,
+    pub drhbhid: u32,
+    pub dreteid: u32,
+    pub avplist: u16,
+}
+
+// Wire format: 24 bytes minimum
+impl DiamGHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 24;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<DiamGHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::diamg,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse DiamG fields from slice")
+    }
+}
