--- /dev/null
+++ b/src/proto_audit/zigbee_aps.rs
@@ -0,0 +1,41 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: ZigbeeAppDataPayload
+// Minimum header: 13 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ZigbeeAppDataPayloadHeader {
+    /// Packed bitfield:
+    ///   frame_control (4 bits)
+    ///   delivery_mode (2 bits)
+    ///   aps_frametype (2 bits)
+    pub frame_control_raw: u8,
+    pub dst_endpoint: u8,
+    pub group_addr: u16,
+    pub cluster: u16,
+    pub profile: u16,
+    pub src_endpoint: u8,
+    pub counter: u8,
+    pub fragmentation: u8,
+    pub block_number: u8,
+    pub ack_bitfield: u8,
+}
+
+// Wire format: 13 bytes minimum
+impl ZigbeeAppDataPayloadHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 13;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<ZigbeeAppDataPayloadHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::zigbeeappdatapayload,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse ZigbeeAppDataPayload fields from slice")
+    }
+}
