--- /dev/null
+++ b/src/proto_audit/rarp.rs
@@ -0,0 +1,36 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: ARP
+// Minimum header: 16 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ARPHeader {
+    pub hwtype: u16,
+    pub ptype: u16,
+    pub hwlen: u8,
+    pub plen: u8,
+    pub op: u16,
+    pub hwsrc: u16,
+    pub psrc: u16,
+    pub hwdst: u16,
+    pub pdst: u16,
+}
+
+// Wire format: 16 bytes minimum
+impl ARPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 16;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<ARPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::arp,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse ARP fields from slice")
+    }
+}
