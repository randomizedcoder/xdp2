--- /dev/null
+++ b/src/proto_audit/sctp_chunk.rs
@@ -0,0 +1,41 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: SCTPChunkData
+// Minimum header: 18 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct SCTPChunkDataHeader {
+    pub r#type: u8,
+    /// Packed bitfield:
+    ///   reserved (4 bits)
+    ///   delay_sack (1 bits)
+    ///   unordered (1 bits)
+    ///   beginning (1 bits)
+    ///   ending (1 bits)
+    pub reserved_raw: u8,
+    pub len: u16,
+    pub tsn: u32,
+    pub stream_id: u16,
+    pub stream_seq: u16,
+    pub proto_id: u32,
+    pub data: u16,
+}
+
+// Wire format: 18 bytes minimum
+impl SCTPChunkDataHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 18;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<SCTPChunkDataHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::sctpchunkdata,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse SCTPChunkData fields from slice")
+    }
+}
