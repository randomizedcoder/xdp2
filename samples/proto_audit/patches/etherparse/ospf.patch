--- /dev/null
+++ b/src/proto_audit/ospf.rs
@@ -0,0 +1,39 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: OSPF_Hdr
+// Minimum header: 32 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct OSPF_HdrHeader {
+    pub version: u8,
+    pub r#type: u8,
+    pub len: u16,
+    pub src: [u8; 4],
+    pub area: [u8; 4],
+    pub chksum: u16,
+    pub authtype: u16,
+    pub authdata: u64,
+    pub reserved: u16,
+    pub keyid: u8,
+    pub authdatalen: u8,
+    pub seq: u32,
+}
+
+// Wire format: 32 bytes minimum
+impl OSPF_HdrHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 32;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<OSPF_HdrHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ospf_hdr,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse OSPF_Hdr fields from slice")
+    }
+}
