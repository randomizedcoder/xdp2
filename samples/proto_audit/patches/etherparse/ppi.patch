--- /dev/null
+++ b/src/proto_audit/ppi.rs
@@ -0,0 +1,13 @@
+//! Ppi header — 84 bytes.
+
+/// Ppi header — 84 bytes.
+pub struct PpiHeader {
+    /// Version: 0.
+    pub ppi_version: u8,
+    /// Flags: 0x00.
+    pub ppi_flags: u8,
+    /// Header length: 84.
+    pub ppi_length: u16,
+    /// DLT: 105.
+    pub ppi_dlt: u32,
+}
