--- /dev/null
+++ b/src/proto_audit/mqtt.rs
@@ -0,0 +1,34 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: MQTT
+// Minimum header: 3 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct MQTTHeader {
+    /// Packed bitfield:
+    ///   type (4 bits)
+    ///   DUP (1 bits)
+    ///   QOS (2 bits)
+    ///   RETAIN (1 bits)
+    pub r#type_raw: u8,
+    pub len: u16,
+}
+
+// Wire format: 3 bytes minimum
+impl MQTTHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 3;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<MQTTHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::mqtt,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse MQTT fields from slice")
+    }
+}
