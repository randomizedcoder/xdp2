--- /dev/null
+++ b/src/proto_audit/rtp.rs
@@ -0,0 +1,23 @@
+//! RTP header (RFC 3550) — 12 bytes.
+
+/// RTP header (RFC 3550) — 12 bytes.
+pub struct RtpHeader {
+    /// Version.
+    pub version: Bits2,
+    /// Padding.
+    pub padding: bool,
+    /// Extension.
+    pub extension: bool,
+    /// CSRC count.
+    pub csrc_count: Bits4,
+    /// Marker.
+    pub marker: bool,
+    /// Payload type.
+    pub payload_type: Bits7,
+    /// Sequence number.
+    pub sequence_number: u16,
+    /// Timestamp.
+    pub timestamp: u32,
+    /// Synchronization source.
+    pub ssrc: u32,
+}
