--- /dev/null
+++ b/src/proto_audit/nvme.rs
@@ -0,0 +1,38 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: NVMe
+// Minimum header: 44 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct NVMeHeader {
+    pub opcode: u8,
+    pub flags: u8,
+    pub command_id: u16,
+    pub nsid: u32,
+    pub cdw2: u64,
+    pub metadata: u64,
+    pub cdw11: u32,
+    pub cdw12: u32,
+    pub cdw13: u32,
+    pub cdw14: u32,
+    pub cdw15: u32,
+}
+
+// Wire format: 44 bytes minimum
+impl NVMeHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 44;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<NVMeHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::nvme,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse NVMe fields from slice")
+    }
+}
