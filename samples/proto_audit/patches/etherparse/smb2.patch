--- /dev/null
+++ b/src/proto_audit/smb2.rs
@@ -0,0 +1,41 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: SMB2_Header
+// Minimum header: 72 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct SMB2_HeaderHeader {
+    pub start: u32,
+    pub structuresize: u16,
+    pub creditcharge: u16,
+    pub status: u32,
+    pub command: u16,
+    pub creditrequest: u16,
+    pub flags: u32,
+    pub nextcommand: u32,
+    pub mid: u64,
+    pub asyncid: u64,
+    pub pid: u32,
+    pub tid: u32,
+    pub sessionid: u64,
+    pub securitysignature: u128,
+}
+
+// Wire format: 72 bytes minimum
+impl SMB2_HeaderHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 72;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<SMB2_HeaderHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::smb2_header,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse SMB2_Header fields from slice")
+    }
+}
