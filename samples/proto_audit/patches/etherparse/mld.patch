--- /dev/null
+++ b/src/proto_audit/mld.rs
@@ -0,0 +1,33 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: ICMPv6MLQuery
+// Minimum header: 24 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ICMPv6MLQueryHeader {
+    pub r#type: u8,
+    pub code: u8,
+    pub cksum: u16,
+    pub mrd: u16,
+    pub reserved: u16,
+    pub mladdr: [u8; 16],
+}
+
+// Wire format: 24 bytes minimum
+impl ICMPv6MLQueryHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 24;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<ICMPv6MLQueryHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::icmpv6mlquery,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse ICMPv6MLQuery fields from slice")
+    }
+}
