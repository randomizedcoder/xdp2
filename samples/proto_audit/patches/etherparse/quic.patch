--- /dev/null
+++ b/src/proto_audit/quic.rs
@@ -0,0 +1,43 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: QUIC_Initial
+// Minimum header: 18 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct QUIC_InitialHeader {
+    /// Packed bitfield:
+    ///   HeaderForm (1 bits)
+    ///   FixedBit (1 bits)
+    ///   LongPacketType (2 bits)
+    ///   Reserved (2 bits)
+    ///   PacketNumberLen (2 bits)
+    pub headerform_raw: u8,
+    pub version: u32,
+    pub dstconnidlen: u8,
+    pub dstconnid: u16,
+    pub srcconnidlen: u8,
+    pub srcconnid: u16,
+    pub tokenlen: u16,
+    pub token: u16,
+    pub length: u16,
+    pub packetnumber: u8,
+}
+
+// Wire format: 18 bytes minimum
+impl QUIC_InitialHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 18;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<QUIC_InitialHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::quic_initial,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse QUIC_Initial fields from slice")
+    }
+}
