--- /dev/null
+++ b/src/proto_audit/iec_sv.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IEC_SV
+// Minimum header: 8 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IEC_SVHeader {
+}
+
+// Wire format: 8 bytes minimum
+impl IEC_SVHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 8;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IEC_SVHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::iec_sv,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IEC_SV fields from slice")
+    }
+}
