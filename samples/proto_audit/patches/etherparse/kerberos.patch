--- /dev/null
+++ b/src/proto_audit/kerberos.rs
@@ -0,0 +1,28 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: Kerberos
+// Minimum header: 0 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct KerberosHeader {
+    pub root: bool,
+}
+
+// Wire format: 0 bytes minimum
+impl KerberosHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 0;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<KerberosHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::kerberos,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse Kerberos fields from slice")
+    }
+}
