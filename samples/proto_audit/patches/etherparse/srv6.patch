--- /dev/null
+++ b/src/proto_audit/srv6.rs
@@ -0,0 +1,43 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IPv6ExtHdrSegmentRouting
+// Minimum header: 12 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IPv6ExtHdrSegmentRoutingHeader {
+    pub nh: u8,
+    pub len: u8,
+    pub r#type: u8,
+    pub segleft: u8,
+    pub lastentry: u8,
+    /// Packed bitfield:
+    ///   unused1 (1 bits)
+    ///   protected (1 bits)
+    ///   oam (1 bits)
+    ///   alert (1 bits)
+    ///   hmac (1 bits)
+    ///   unused2 (3 bits)
+    pub unused1_raw: u8,
+    pub tag: u16,
+    pub addresses: u16,
+    pub tlv_objects: u16,
+}
+
+// Wire format: 12 bytes minimum
+impl IPv6ExtHdrSegmentRoutingHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 12;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IPv6ExtHdrSegmentRoutingHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ipv6exthdrsegmentrouting,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IPv6ExtHdrSegmentRouting fields from slice")
+    }
+}
