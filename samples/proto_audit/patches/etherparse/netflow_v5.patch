--- /dev/null
+++ b/src/proto_audit/netflow_v5.rs
@@ -0,0 +1,35 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: NetflowHeaderV5
+// Minimum header: 22 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct NetflowHeaderV5Header {
+    pub count: u16,
+    pub sysuptime: u32,
+    pub unixsecs: u32,
+    pub unixnanoseconds: u32,
+    pub flowsequence: u32,
+    pub enginetype: u8,
+    pub engineid: u8,
+    pub samplinginterval: u16,
+}
+
+// Wire format: 22 bytes minimum
+impl NetflowHeaderV5Header {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 22;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<NetflowHeaderV5Header, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::netflowheaderv5,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse NetflowHeaderV5 fields from slice")
+    }
+}
