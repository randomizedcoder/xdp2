--- /dev/null
+++ b/src/proto_audit/ldap.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: LDAP
+// Minimum header: 1 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct LDAPHeader {
+}
+
+// Wire format: 1 bytes minimum
+impl LDAPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 1;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<LDAPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ldap,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse LDAP fields from slice")
+    }
+}
