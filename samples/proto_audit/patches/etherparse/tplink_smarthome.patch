--- /dev/null
+++ b/src/proto_audit/tplink_smarthome.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: TPLINK_SMARTHOME
+// Minimum header: 4 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct TPLINK_SMARTHOMEHeader {
+}
+
+// Wire format: 4 bytes minimum
+impl TPLINK_SMARTHOMEHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 4;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<TPLINK_SMARTHOMEHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::tplink_smarthome,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse TPLINK_SMARTHOME fields from slice")
+    }
+}
