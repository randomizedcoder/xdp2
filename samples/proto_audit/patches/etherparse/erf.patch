--- /dev/null
+++ b/src/proto_audit/erf.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: ERF
+// Minimum header: 18 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ERFHeader {
+}
+
+// Wire format: 18 bytes minimum
+impl ERFHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 18;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<ERFHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::erf,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse ERF fields from slice")
+    }
+}
