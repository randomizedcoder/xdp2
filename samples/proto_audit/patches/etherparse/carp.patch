--- /dev/null
+++ b/src/proto_audit/carp.rs
@@ -0,0 +1,40 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: VRRPv3
+// Minimum header: 10 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct VRRPv3Header {
+    /// Packed bitfield:
+    ///   version (4 bits)
+    ///   type (4 bits)
+    pub version_raw: u8,
+    pub vrid: u8,
+    pub priority: u8,
+    pub ipcount: u8,
+    /// Packed bitfield:
+    ///   res (4 bits)
+    pub res_raw: u8,
+    pub adv: u16,
+    pub chksum: u16,
+    pub addrlist: u16,
+}
+
+// Wire format: 10 bytes minimum
+impl VRRPv3Header {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 10;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<VRRPv3Header, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::vrrpv3,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse VRRPv3 fields from slice")
+    }
+}
