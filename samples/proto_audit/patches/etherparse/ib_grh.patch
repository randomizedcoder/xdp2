--- /dev/null
+++ b/src/proto_audit/ib_grh.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IB_GRH
+// Minimum header: 40 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IB_GRHHeader {
+}
+
+// Wire format: 40 bytes minimum
+impl IB_GRHHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 40;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IB_GRHHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ib_grh,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IB_GRH fields from slice")
+    }
+}
