--- /dev/null
+++ b/src/proto_audit/aoe.rs
@@ -0,0 +1,40 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: AOE
+// Minimum header: 18 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct AOEHeader {
+    /// Packed bitfield:
+    ///   version (4 bits)
+    ///   flags (4 bits)
+    pub version_raw: u8,
+    pub error: u8,
+    pub major: u16,
+    pub minor: u8,
+    pub cmd: u8,
+    pub tag: u32,
+    pub i_ata_cmd: u16,
+    pub q_conf_info: u16,
+    pub mac_m_list: u16,
+    pub res_rel: u16,
+}
+
+// Wire format: 18 bytes minimum
+impl AOEHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 18;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<AOEHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::aoe,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse AOE fields from slice")
+    }
+}
