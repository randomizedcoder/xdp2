--- /dev/null
+++ b/src/proto_audit/ipv6_fragment.rs
@@ -0,0 +1,35 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IPv6ExtHdrFragment
+// Minimum header: 8 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IPv6ExtHdrFragmentHeader {
+    pub nh: u8,
+    pub res1: u8,
+    pub offset: u16,
+    /// Packed bitfield:
+    ///   res2 (2 bits)
+    ///   m (1 bits)
+    pub res2_raw: u8,
+    pub id: u32,
+}
+
+// Wire format: 8 bytes minimum
+impl IPv6ExtHdrFragmentHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 8;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IPv6ExtHdrFragmentHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ipv6exthdrfragment,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IPv6ExtHdrFragment fields from slice")
+    }
+}
