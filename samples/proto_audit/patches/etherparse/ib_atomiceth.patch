--- /dev/null
+++ b/src/proto_audit/ib_atomiceth.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IB_AtomicETH
+// Minimum header: 28 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IB_AtomicETHHeader {
+}
+
+// Wire format: 28 bytes minimum
+impl IB_AtomicETHHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 28;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IB_AtomicETHHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ib_atomiceth,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IB_AtomicETH fields from slice")
+    }
+}
