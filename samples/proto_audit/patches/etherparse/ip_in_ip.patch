--- /dev/null
+++ b/src/proto_audit/ip_in_ip.rs
@@ -0,0 +1,44 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IP
+// Minimum header: 22 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IPHeader {
+    /// Packed bitfield:
+    ///   version (4 bits)
+    ///   ihl (4 bits)
+    pub version_raw: u8,
+    pub tos: u8,
+    pub len: u16,
+    pub id: u16,
+    /// Packed bitfield:
+    ///   flags (3 bits)
+    pub flags_raw: u8,
+    pub frag: u16,
+    pub ttl: u8,
+    pub proto: u8,
+    pub chksum: u16,
+    pub src: [u8; 4],
+    pub dst: [u8; 4],
+    pub options: u16,
+}
+
+// Wire format: 22 bytes minimum
+impl IPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 22;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ip,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IP fields from slice")
+    }
+}
