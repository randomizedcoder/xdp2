--- /dev/null
+++ b/src/proto_audit/lwapp.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: LWAPP
+// Minimum header: 6 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct LWAPPHeader {
+}
+
+// Wire format: 6 bytes minimum
+impl LWAPPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 6;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<LWAPPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::lwapp,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse LWAPP fields from slice")
+    }
+}
