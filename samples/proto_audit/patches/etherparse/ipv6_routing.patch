--- /dev/null
+++ b/src/proto_audit/ipv6_routing.rs
@@ -0,0 +1,33 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IPv6ExtHdrRouting
+// Minimum header: 10 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IPv6ExtHdrRoutingHeader {
+    pub nh: u8,
+    pub len: u8,
+    pub r#type: u8,
+    pub segleft: u8,
+    pub reserved: u32,
+    pub addresses: u16,
+}
+
+// Wire format: 10 bytes minimum
+impl IPv6ExtHdrRoutingHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 10;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IPv6ExtHdrRoutingHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ipv6exthdrrouting,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IPv6ExtHdrRouting fields from slice")
+    }
+}
