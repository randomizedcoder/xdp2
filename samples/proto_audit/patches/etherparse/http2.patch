--- /dev/null
+++ b/src/proto_audit/http2.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: HTTP2
+// Minimum header: 9 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct HTTP2Header {
+}
+
+// Wire format: 9 bytes minimum
+impl HTTP2Header {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 9;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<HTTP2Header, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::http2,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse HTTP2 fields from slice")
+    }
+}
