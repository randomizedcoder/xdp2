--- /dev/null
+++ b/src/proto_audit/bt_bnep.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: BT_BNEP
+// Minimum header: 3 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct BT_BNEPHeader {
+}
+
+// Wire format: 3 bytes minimum
+impl BT_BNEPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 3;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<BT_BNEPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::bt_bnep,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse BT_BNEP fields from slice")
+    }
+}
