--- /dev/null
+++ b/src/proto_audit/appletalk.rs
@@ -0,0 +1,29 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: AppleTalk
+// Minimum header: 3 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct AppleTalkHeader {
+    pub s_net: u16,
+    pub s_node: u8,
+}
+
+// Wire format: 3 bytes minimum
+impl AppleTalkHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 3;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<AppleTalkHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::appletalk,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse AppleTalk fields from slice")
+    }
+}
