--- /dev/null
+++ b/src/proto_audit/rip.rs
@@ -0,0 +1,30 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: RIP
+// Minimum header: 4 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct RIPHeader {
+    pub cmd: u8,
+    pub version: u8,
+    pub null: u16,
+}
+
+// Wire format: 4 bytes minimum
+impl RIPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 4;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<RIPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::rip,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse RIP fields from slice")
+    }
+}
