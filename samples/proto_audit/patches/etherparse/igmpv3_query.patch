--- /dev/null
+++ b/src/proto_audit/igmpv3_query.rs
@@ -0,0 +1,38 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IGMPv3_Query
+// Minimum header: 12 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IGMPv3_QueryHeader {
+    pub r#type: u8,
+    pub code: u8,
+    pub csum: u16,
+    pub group: [u8; 4],
+    /// Packed bitfield:
+    ///   resv (4 bits)
+    ///   suppress (1 bits)
+    ///   qrv (3 bits)
+    pub resv_raw: u8,
+    pub qqic: u8,
+    pub nsrcs: u16,
+}
+
+// Wire format: 12 bytes minimum
+impl IGMPv3_QueryHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 12;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IGMPv3_QueryHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::igmpv3_query,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IGMPv3_Query fields from slice")
+    }
+}
