--- /dev/null
+++ b/src/proto_audit/phonet.rs
@@ -0,0 +1,33 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: Phonet
+// Minimum header: 7 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct PhonetHeader {
+    pub pn_rdev: u8,
+    pub pn_sdev: u8,
+    pub pn_res: u8,
+    pub pn_length: u16,
+    pub pn_robj: u8,
+    pub pn_sobj: u8,
+}
+
+// Wire format: 7 bytes minimum
+impl PhonetHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 7;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<PhonetHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::phonet,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse Phonet fields from slice")
+    }
+}
