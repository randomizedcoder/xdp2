--- /dev/null
+++ b/src/proto_audit/ethercat.rs
@@ -0,0 +1,32 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: EtherCat
+// Minimum header: 2 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct EtherCatHeader {
+    pub length: u16,
+    /// Packed bitfield:
+    ///   _reserved (1 bits)
+    ///   type (4 bits)
+    pub _reserved_raw: u8,
+}
+
+// Wire format: 2 bytes minimum
+impl EtherCatHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 2;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<EtherCatHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ethercat,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse EtherCat fields from slice")
+    }
+}
