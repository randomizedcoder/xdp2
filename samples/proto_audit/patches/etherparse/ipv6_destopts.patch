--- /dev/null
+++ b/src/proto_audit/ipv6_destopts.rs
@@ -0,0 +1,31 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IPv6ExtHdrDestOpt
+// Minimum header: 5 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IPv6ExtHdrDestOptHeader {
+    pub nh: u8,
+    pub len: u8,
+    pub autopad: u8,
+    pub options: u16,
+}
+
+// Wire format: 5 bytes minimum
+impl IPv6ExtHdrDestOptHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 5;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IPv6ExtHdrDestOptHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ipv6exthdrdestopt,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IPv6ExtHdrDestOpt fields from slice")
+    }
+}
