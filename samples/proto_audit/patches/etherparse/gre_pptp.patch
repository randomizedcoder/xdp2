--- /dev/null
+++ b/src/proto_audit/gre_pptp.rs
@@ -0,0 +1,43 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: GRE_PPTP
+// Minimum header: 16 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct GRE_PPTPHeader {
+    /// Packed bitfield:
+    ///   chksum_present (1 bits)
+    ///   routing_present (1 bits)
+    ///   key_present (1 bits)
+    ///   seqnum_present (1 bits)
+    ///   strict_route_source (1 bits)
+    ///   recursion_control (3 bits)
+    ///   acknum_present (1 bits)
+    ///   flags (4 bits)
+    ///   version (3 bits)
+    pub chksum_present_raw: u16,
+    pub proto: u16,
+    pub payload_len: u16,
+    pub call_id: u16,
+    pub sequence_number: u32,
+    pub ack_number: u32,
+}
+
+// Wire format: 16 bytes minimum
+impl GRE_PPTPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 16;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<GRE_PPTPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::gre_pptp,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse GRE_PPTP fields from slice")
+    }
+}
