--- /dev/null
+++ b/src/proto_audit/mpeg_ts.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: MPEG_TS
+// Minimum header: 188 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct MPEG_TSHeader {
+}
+
+// Wire format: 188 bytes minimum
+impl MPEG_TSHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 188;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<MPEG_TSHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::mpeg_ts,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse MPEG_TS fields from slice")
+    }
+}
