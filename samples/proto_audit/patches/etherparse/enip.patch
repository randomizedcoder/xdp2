--- /dev/null
+++ b/src/proto_audit/enip.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: ENIP
+// Minimum header: 24 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ENIPHeader {
+}
+
+// Wire format: 24 bytes minimum
+impl ENIPHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 24;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<ENIPHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::enip,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse ENIP fields from slice")
+    }
+}
