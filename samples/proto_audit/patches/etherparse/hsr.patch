--- /dev/null
+++ b/src/proto_audit/hsr.rs
@@ -0,0 +1,11 @@
+//! HSR tag (IEC 62439-3) — 6 bytes.
+
+/// HSR tag (IEC 62439-3) — 6 bytes.
+pub struct HsrHeader {
+    /// Path + LSDU size.
+    pub path_lsdu_size: u16,
+    /// Sequence number.
+    pub seq_nr: u16,
+    /// Encapsulated protocol (EtherType).
+    pub ether_type: u16,
+}
