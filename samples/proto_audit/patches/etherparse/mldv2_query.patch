--- /dev/null
+++ b/src/proto_audit/mldv2_query.rs
@@ -0,0 +1,41 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: ICMPv6MLQuery2
+// Minimum header: 30 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct ICMPv6MLQuery2Header {
+    pub r#type: u8,
+    pub code: u8,
+    pub cksum: u16,
+    pub mrd: u16,
+    pub reserved: u16,
+    pub mladdr: [u8; 16],
+    /// Packed bitfield:
+    ///   Resv (4 bits)
+    ///   S (1 bits)
+    ///   QRV (3 bits)
+    pub resv_raw: u8,
+    pub qqic: u8,
+    pub sources_number: u16,
+    pub sources: u16,
+}
+
+// Wire format: 30 bytes minimum
+impl ICMPv6MLQuery2Header {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 30;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<ICMPv6MLQuery2Header, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::icmpv6mlquery2,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse ICMPv6MLQuery2 fields from slice")
+    }
+}
