--- /dev/null
+++ b/src/proto_audit/ib_bth.rs
@@ -0,0 +1,27 @@
+// Auto-generated by proto-audit from IR definition.
+// Protocol: IB_BTH
+// Minimum header: 12 bytes
+
+#[derive(Debug, Clone, PartialEq, Eq)]
+pub struct IB_BTHHeader {
+}
+
+// Wire format: 12 bytes minimum
+impl IB_BTHHeader {
+    /// Minimum header size in bytes.
+    pub const MIN_LEN: usize = 12;
+
+    /// Read from a byte slice.
+    pub fn from_slice(slice: &[u8]) -> Result<IB_BTHHeader, err::LenError> {
+        if slice.len() < Self::MIN_LEN {
+            return Err(err::LenError {
+                required_len: Self::MIN_LEN,
+                len: slice.len(),
+                len_source: err::LenSource::Slice,
+                layer: err::Layer::ib_bth,
+                layer_start_offset: 0,
+            });
+        }
+        todo!("parse IB_BTH fields from slice")
+    }
+}
