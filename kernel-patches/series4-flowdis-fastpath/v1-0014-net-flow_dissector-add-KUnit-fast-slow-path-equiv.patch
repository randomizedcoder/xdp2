From bc3295552e8f46a8c30052344f997b7a500f58f8 Mon Sep 17 00:00:00 2001
From: Dave Seddon <dave.seddon.ca@gmail.com>
Date: Sun, 5 Jul 2026 17:15:20 -0700
Subject: [PATCH net-next v1 14/15] net: flow_dissector: add KUnit fast/slow
 path equivalence tests

The per-shape fast paths contract to be byte-identical with the slow
path: for any packet, either struct flow_keys is filled with exactly
the bytes the slow path would have written, or the fast path returns
false and the slow path runs. Until now that contract was only
verified out of tree (a userspace A/B harness compiling the dissector
both ways). Enforce it in-tree so any future divergence between the
two implementations is a failing test, not a silent behaviour change.

Each corpus packet is dissected twice into zeroed containers - once
with every fast-path gate disabled and once with every byte-identical
gate enabled - and the return values and full struct flow_keys must
match exactly (the zeroed containers also prove a fast-path miss never
leaves partial writes behind). The corpus covers the eligible shapes
(eth+IPv4/IPv6 x TCP/UDP, VLAN, QinQ, PPPoE session, single MPLS,
IPIP/4in6/6in4, plain GRE), deliberate fast-path misses (IP options,
fragments with and without FLOW_DISSECTOR_F_PARSE_1ST_FRAG, IPv6
extension headers and non-zero flow labels, triple VLAN, multi-label
MPLS, non-IP PPP, flagged GRE, ICMP), a truncation sweep cutting every
packet at every byte boundary, and skb-mode cases for the
hardware-stripped VLAN tag entry conditions raw-data mode cannot
reach.

The UDP-tunnel descents (vxlan/geneve/gtpu/fou/gue inner) are covered
too. Because a descent changes the output relative to not descending,
the equivalence is checked the other way round: the descent gate is
pinned on and the entry gates are toggled, so one pass descends via the
fast path and the other descends via the slow path (the fast
dispatcher defers with the entry gates off). Both must produce the same
flow_keys. The corpus exercises inner TCP/UDP, IPv6-outer/IPv4-inner
residue for fou/gue, inner ICMP and Geneve/GTP-U out-of-subset packets
(both keep the outer keys), and a nested VXLAN chain past
MAX_FLOW_DISSECT_HDRS (both bail at the same depth). A dummy netdev and
a mock flow_dissector_fou_ops provider drive the FOU/GUE lookup without
real fou config.

Every equivalence case runs both with and without
FLOW_DISSECTOR_F_STOP_AT_FLOW_LABEL -- the flag skb_get_hash()
passes on every dissect -- against both dissectors, and the
gates-off test's positive control asserts the fast path actually
hits for that flagship flag shape (it caught the dispatcher
rejecting the flag outright, which had made the fast path
unreachable from RPS/RFS/fq/cake).

Beyond byte equality, every check also asserts
flow_hash_from_keys() equality (the hash is what consumers act on),
and dedicated cases pin down the control surfaces: STOP_AT_ENCAP /
STOP_BEFORE_ENCAP callers keep the outer tuple even with a descent
gate on; a gates-off pass over the whole corpus proves the fast path
never runs when disabled (observed via the per-shape counters); and
the descent header validation is exercised negatively (VXLAN with the
I flag clear or GBP bits set, Geneve OAM frames, GTP-U with an option
flag set -- all must keep the outer keys on both paths). A seeded,
reproducible fuzzer mutates corpus packets and re-checks equivalence
every iteration, as a guard for divergences the hand-written corpus
does not name.

Run with:

  ./tools/testing/kunit/kunit.py run --arch=x86_64 \
      --kconfig_add CONFIG_NET=y \
      --kconfig_add CONFIG_FLOW_DISSECTOR_KUNIT_TEST=y \
      flow_dissector_fastpath

Assisted-by: Claude:claude-fable-5 sparse
Signed-off-by: Dave Seddon <dave.seddon.ca@gmail.com>
---
 include/net/flow_dissector.h   |    9 +
 net/Kconfig                    |   12 +
 net/core/Makefile              |    1 +
 net/core/flow_dissector.c      |   37 +-
 net/core/flow_dissector_test.c | 1722 ++++++++++++++++++++++++++++++++
 5 files changed, 1777 insertions(+), 4 deletions(-)
 create mode 100644 net/core/flow_dissector_test.c

diff --git a/include/net/flow_dissector.h b/include/net/flow_dissector.h
index 476c17cc7249..4dbe93c7e06c 100644
--- a/include/net/flow_dissector.h
+++ b/include/net/flow_dissector.h
@@ -485,6 +485,15 @@ enum flow_dissector_shape {
 	FLOW_DISSECTOR_SHAPE__MAX,
 };
 
+#if IS_ENABLED(CONFIG_FLOW_DISSECTOR_KUNIT_TEST)
+/* Test-only accessors, defined in flow_dissector.c: the file-static
+ * symmetric dissector (the second dissector the fast path admits) and
+ * the summed fast-hit counters (observable for the gates-off negative).
+ */
+struct flow_dissector *flow_keys_dissector_symmetric_kunit(void);
+u64 flow_dissector_fast_hits_kunit(void);
+#endif
+
 /* struct flow_keys_digest:
  *
  * This structure is used to hold a digest of the full flow keys. This is a
diff --git a/net/Kconfig b/net/Kconfig
index e38477393551..d0ce8fc34429 100644
--- a/net/Kconfig
+++ b/net/Kconfig
@@ -540,4 +540,16 @@ config NET_TEST
 
 	  If unsure, say N.
 
+config FLOW_DISSECTOR_KUNIT_TEST
+	tristate "KUnit tests for flow dissector fast/slow path equivalence" if !KUNIT_ALL_TESTS
+	depends on KUNIT
+	default KUNIT_ALL_TESTS
+	help
+	  KUnit tests asserting the flow dissector's opt-in per-shape
+	  fast paths produce byte-identical struct flow_keys output to
+	  the slow path for every corpus packet, with the gates on and
+	  off.
+
+	  If unsure, say N.
+
 endif   # if NET
diff --git a/net/core/Makefile b/net/core/Makefile
index b3fdcb4e355f..b27c69580d2c 100644
--- a/net/core/Makefile
+++ b/net/core/Makefile
@@ -46,6 +46,7 @@ obj-$(CONFIG_BPF_SYSCALL) += sock_map.o
 obj-$(CONFIG_BPF_SYSCALL) += bpf_sk_storage.o
 obj-$(CONFIG_OF)	+= of_net.o
 obj-$(CONFIG_NET_TEST) += net_test.o
+obj-$(CONFIG_FLOW_DISSECTOR_KUNIT_TEST) += flow_dissector_test.o
 obj-$(CONFIG_NET_DEVMEM) += devmem.o
 obj-$(CONFIG_DEBUG_NET) += lock_debug.o
 obj-$(CONFIG_FAIL_SKB_REALLOC) += skb_fault_injection.o
diff --git a/net/core/flow_dissector.c b/net/core/flow_dissector.c
index dcb327d94870..3085cf6f07f8 100644
--- a/net/core/flow_dissector.c
+++ b/net/core/flow_dissector.c
@@ -1773,10 +1773,6 @@ static bool flow_dissect_fast_ipv6(const struct sk_buff *skb,
 				nhoff + (int)sizeof(*iph), hlen, num_hdrs);
 	}
 
-	if (unlikely(iph->nexthdr != IPPROTO_TCP &&
-		     iph->nexthdr != IPPROTO_UDP))
-		return false;
-
 	thoff = nhoff + (int)sizeof(*iph);
 
 	if (dissector_uses_key(flow_dissector,
@@ -3652,6 +3648,39 @@ EXPORT_SYMBOL(flow_keys_dissector);
 struct flow_dissector flow_keys_basic_dissector __read_mostly;
 EXPORT_SYMBOL(flow_keys_basic_dissector);
 
+#if IS_ENABLED(CONFIG_FLOW_DISSECTOR_KUNIT_TEST)
+/* The symmetric dissector is file-static; the fast/slow equivalence
+ * KUnit suite needs it to exercise the dispatcher's eligibility gate
+ * for the second standard dissector and its different used_keys set
+ * (no VLAN, no flow label). Test-only accessor.
+ */
+struct flow_dissector *flow_keys_dissector_symmetric_kunit(void)
+{
+	return &flow_keys_dissector_symmetric;
+}
+EXPORT_SYMBOL_GPL(flow_keys_dissector_symmetric_kunit);
+
+/* Sum of the per-shape fast-path hit counters, for the gates-off
+ * negative test (with every gate off the fast path must never run,
+ * and the counters are the observable). Test-only accessor.
+ */
+u64 flow_dissector_fast_hits_kunit(void)
+{
+	u64 sum = 0;
+	int cpu, i;
+
+	for_each_possible_cpu(cpu) {
+		const struct flow_dissector_stats *s =
+			per_cpu_ptr(&flow_dissector_pcpu_stats, cpu);
+
+		for (i = 0; i < FLOW_DISSECTOR_SHAPE__MAX; i++)
+			sum += s->fast_hits[i];
+	}
+	return sum;
+}
+EXPORT_SYMBOL_GPL(flow_dissector_fast_hits_kunit);
+#endif
+
 static int __init init_default_flow_dissectors(void)
 {
 	skb_flow_dissector_init(&flow_keys_dissector,
diff --git a/net/core/flow_dissector_test.c b/net/core/flow_dissector_test.c
new file mode 100644
index 000000000000..00893dd6d30a
--- /dev/null
+++ b/net/core/flow_dissector_test.c
@@ -0,0 +1,1722 @@
+// SPDX-License-Identifier: GPL-2.0-or-later
+/*
+ * KUnit equivalence tests for the flow dissector opt-in fast paths.
+ *
+ * Every fast-path shape is gated behind a static key and contracts to
+ * be byte-identical with the slow path: for any packet it either fills
+ * struct flow_keys with exactly the bytes the slow path would have
+ * written, or it returns false and the slow path runs. These tests
+ * enforce that contract: each packet is dissected twice into zeroed
+ * containers - once with every fast-path gate disabled (slow path) and
+ * once with every byte-identical gate enabled - and the two containers
+ * and return values must match exactly. Divergence between the two
+ * implementations is therefore a test failure, not a silent behaviour
+ * change.
+ *
+ * The corpus covers the eligible shapes (eth+IPv4/IPv6 x TCP/UDP,
+ * VLAN, QinQ, PPPoE session, single MPLS, IPIP/4in6/6in4, plain GRE)
+ * plus deliberate fast-path misses (IP options, fragments, IPv6
+ * extension headers and flow labels, deep VLAN stacks, multi-label
+ * MPLS, non-IP PPP, flagged GRE, ICMP) and a truncation sweep that
+ * cuts every packet at each possible byte boundary. Zeroed containers
+ * also prove a fast-path miss never leaves partial writes behind.
+ *
+ * Every check runs against both eligible dissectors:
+ * flow_keys_dissector and flow_keys_dissector_symmetric (file-static
+ * in flow_dissector.c, reached through a test-only accessor) — their
+ * different used_keys sets exercise different fast-path branches.
+ */
+
+#include <kunit/test.h>
+
+#include <linux/etherdevice.h>
+#include <linux/if_ether.h>
+#include <linux/if_pppox.h>
+#include <linux/if_tunnel.h>
+#include <linux/if_vlan.h>
+#include <linux/ip.h>
+#include <linux/ipv6.h>
+#include <linux/mpls.h>
+#include <linux/netdevice.h>
+#include <linux/ppp_defs.h>
+#include <linux/prandom.h>
+#include <linux/skbuff.h>
+#include <linux/udp.h>
+#include <net/flow_dissector.h>
+#include <net/geneve.h>
+#include <net/gre.h>
+#include <net/gtp.h>
+#include <net/gue.h>
+#include <net/ip.h>
+#include <net/net_namespace.h>
+#include <net/vxlan.h>
+#include <uapi/linux/fou.h>
+
+#define FD_TEST_BUF_LEN 512
+#define FD_FUZZ_ITERS 4000
+
+/* The byte-identical fast-path gates (descent gates excluded: those
+ * change dissection output by design and are never part of the
+ * byte-identical contract).
+ */
+static struct static_key_false * const fd_fast_gates[] = {
+	&flow_dissector_eth_ip_key,
+	&flow_dissector_vlan_key,
+	&flow_dissector_qinq_key,
+	&flow_dissector_pppoe_key,
+	&flow_dissector_mpls_key,
+	&flow_dissector_ipip_key,
+	&flow_dissector_gre_key,
+};
+
+/* The behaviour-changing descent gates: individual tests pin these on
+ * and restore them; suite init/exit clears them too so a failing test
+ * can never leak an enabled gate into its successors.
+ */
+static struct static_key_false * const fd_descent_gates[] = {
+	&flow_dissector_vxlan_inner_key,
+	&flow_dissector_geneve_inner_key,
+	&flow_dissector_gtpu_inner_key,
+	&flow_dissector_gue_inner_key,
+	&flow_dissector_fou_inner_key,
+};
+
+static void fd_fast_gates_set(bool on)
+{
+	int i;
+
+	for (i = 0; i < ARRAY_SIZE(fd_fast_gates); i++) {
+		if (on)
+			static_branch_enable(fd_fast_gates[i]);
+		else
+			static_branch_disable(fd_fast_gates[i]);
+	}
+}
+
+/* --- packet builders (network-layer payloads; __skb_flow_dissect is
+ * called in raw-data mode with the L3 proto passed explicitly, so no
+ * Ethernet header is included) ---
+ */
+
+static int put_ports(u8 *buf)
+{
+	__be16 *p = (__be16 *)buf;
+
+	p[0] = htons(0x1234);	/* sport */
+	p[1] = htons(0x5678);	/* dport */
+	return 4;
+}
+
+static int put_ipv4(u8 *buf, u8 ip_proto, u8 ihl, __be16 frag_off)
+{
+	struct iphdr *ip = (struct iphdr *)buf;
+	int hdrlen = ihl * 4;
+
+	memset(ip, 0, hdrlen);
+	ip->version = 4;
+	ip->ihl = ihl;
+	ip->ttl = 64;
+	ip->tot_len = htons(hdrlen + 4);
+	ip->frag_off = frag_off;
+	ip->protocol = ip_proto;
+	ip->saddr = htonl(0xc0a80101);	/* 192.168.1.1 */
+	ip->daddr = htonl(0xc0a80202);	/* 192.168.2.2 */
+	return hdrlen;
+}
+
+static int put_ipv6(u8 *buf, u8 nexthdr, u32 flow_label)
+{
+	struct ipv6hdr *ip6 = (struct ipv6hdr *)buf;
+
+	memset(ip6, 0, sizeof(*ip6));
+	ip6->version = 6;
+	ip6->flow_lbl[0] = (flow_label >> 16) & 0x0f;
+	ip6->flow_lbl[1] = (flow_label >> 8) & 0xff;
+	ip6->flow_lbl[2] = flow_label & 0xff;
+	ip6->payload_len = htons(4);
+	ip6->nexthdr = nexthdr;
+	ip6->hop_limit = 64;
+	ip6->saddr.s6_addr32[0] = htonl(0x20010db8);
+	ip6->saddr.s6_addr32[3] = htonl(1);
+	ip6->daddr.s6_addr32[0] = htonl(0x20010db8);
+	ip6->daddr.s6_addr32[3] = htonl(2);
+	return sizeof(*ip6);
+}
+
+static int put_vlan(u8 *buf, u16 tci, __be16 inner_proto)
+{
+	struct vlan_hdr *vlan = (struct vlan_hdr *)buf;
+
+	vlan->h_vlan_TCI = htons(tci);
+	vlan->h_vlan_encapsulated_proto = inner_proto;
+	return sizeof(*vlan);
+}
+
+static int put_pppoe(u8 *buf, u16 ppp_proto, u16 payload_len)
+{
+	struct pppoe_hdr *hdr = (struct pppoe_hdr *)buf;
+	__be16 *proto = (__be16 *)(buf + sizeof(*hdr));
+
+	memset(hdr, 0, sizeof(*hdr));
+	hdr->ver = 1;
+	hdr->type = 1;
+	hdr->code = 0;
+	hdr->sid = htons(0x42);
+	hdr->length = htons(payload_len + 2);
+	*proto = htons(ppp_proto);
+	return sizeof(*hdr) + 2;
+}
+
+static int put_mpls(u8 *buf, u32 label, u32 bos)
+{
+	struct mpls_label *lse = (struct mpls_label *)buf;
+
+	lse->entry = htonl((label << MPLS_LS_LABEL_SHIFT) |
+			   (bos << MPLS_LS_S_SHIFT) |
+			   (64 << MPLS_LS_TTL_SHIFT));
+	return sizeof(*lse);
+}
+
+static int put_gre(u8 *buf, __be16 flags, __be16 protocol)
+{
+	struct gre_base_hdr *gre = (struct gre_base_hdr *)buf;
+
+	gre->flags = flags;
+	gre->protocol = protocol;
+	return sizeof(*gre);
+}
+
+/* --- composite builders, one per corpus entry --- */
+
+static int build_ipv4_tcp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_TCP, 5, 0);
+
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv4_udp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_UDP, 5, 0);
+
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv6_tcp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_TCP, 0);
+
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv6_udp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_UDP, 0);
+
+	return n + put_ports(buf + n);
+}
+
+static int build_vlan_ipv4_tcp(u8 *buf)
+{
+	int n = put_vlan(buf, 100, htons(ETH_P_IP));
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_vlan_ipv6_udp(u8 *buf)
+{
+	int n = put_vlan(buf, 100, htons(ETH_P_IPV6));
+
+	n += put_ipv6(buf + n, IPPROTO_UDP, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_qinq_ipv4_tcp(u8 *buf)
+{
+	int n = put_vlan(buf, 100, htons(ETH_P_8021Q));
+
+	n += put_vlan(buf + n, 200, htons(ETH_P_IP));
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_vlan3_ipv4_tcp(u8 *buf)
+{
+	int n = put_vlan(buf, 100, htons(ETH_P_8021Q));
+
+	n += put_vlan(buf + n, 200, htons(ETH_P_8021Q));
+	n += put_vlan(buf + n, 300, htons(ETH_P_IP));
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_pppoe_ipv4_tcp(u8 *buf)
+{
+	int n = put_pppoe(buf, PPP_IP, 20 + 4);
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_pppoe_ipv6_tcp(u8 *buf)
+{
+	int n = put_pppoe(buf, PPP_IPV6, 40 + 4);
+
+	n += put_ipv6(buf + n, IPPROTO_TCP, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_pppoe_lcp(u8 *buf)
+{
+	int n = put_pppoe(buf, PPP_LCP, 4);
+	u8 lcp[4] = { 0x01, 0x01, 0x00, 0x04 };	/* configure-request */
+
+	memcpy(buf + n, lcp, sizeof(lcp));
+	return n + sizeof(lcp);
+}
+
+static int build_mpls_ipv4(u8 *buf)
+{
+	int n = put_mpls(buf, 1000, 1);
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_mpls_multi(u8 *buf)
+{
+	int n = put_mpls(buf, 1000, 0);	/* S=0: not bottom of stack */
+
+	n += put_mpls(buf + n, 2000, 1);
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_ipip(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_IPIP, 5, 0);
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_6in4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_IPV6, 5, 0);
+
+	n += put_ipv6(buf + n, IPPROTO_TCP, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_4in6(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_IPIP, 0);
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_4in6_flowlabel(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_IPIP, 0xbeef);
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_6in6(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_IPV6, 0);
+
+	n += put_ipv6(buf + n, IPPROTO_TCP, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_ipv4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	n += put_gre(buf + n, 0, htons(ETH_P_IP));
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre6_ipv4(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_GRE, 0);
+
+	n += put_gre(buf + n, 0, htons(ETH_P_IP));
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+/* --- corner-case builders --- */
+
+/* GRE variants that must defer (any non-zero flags/version word). */
+static int build_gre_seq_ipv4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	n += put_gre(buf + n, GRE_SEQ, htons(ETH_P_IP));
+	memset(buf + n, 0, 4);		/* sequence number */
+	n += 4;
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_key_ipv4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	n += put_gre(buf + n, GRE_KEY, htons(ETH_P_IP));
+	memset(buf + n, 0, 4);		/* key */
+	n += 4;
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_version1(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	/* GRE version 1 (PPTP) lives in the low bits of the flags word. */
+	n += put_gre(buf + n, htons(0x0001), htons(ETH_P_IP));
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_teb(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	/* Transparent Ethernet bridging inner: fast path defers. */
+	n += put_gre(buf + n, 0, htons(ETH_P_TEB));
+	return n + put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+}
+
+/* An IPv6 extension header (8 bytes, hdrlen field 0). */
+static int put_ext_hdr(u8 *buf, u8 next_proto)
+{
+	memset(buf, 0, 8);
+	buf[0] = next_proto;
+	buf[1] = 0;			/* hdr ext len: (0 + 1) * 8 = 8 bytes */
+	return 8;
+}
+
+static int build_ipv6_hopopts_dest_tcp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_HOPOPTS, 0);
+
+	n += put_ext_hdr(buf + n, IPPROTO_DSTOPTS);
+	n += put_ext_hdr(buf + n, IPPROTO_TCP);
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv6_frag_tcp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_FRAGMENT, 0);
+
+	memset(buf + n, 0, 8);
+	buf[n] = IPPROTO_TCP;		/* fragment header, offset 0 */
+	*(__be32 *)(buf + n + 4) = htonl(0x12345678);
+	n += 8;
+	return n + put_ports(buf + n);
+}
+
+/* VLAN TPID ordering the corpus doesn't otherwise cover: 8021Q then 8021AD. */
+static int build_vlan_q_then_ad_ipv4(u8 *buf)
+{
+	int n = put_vlan(buf, 100, htons(ETH_P_8021AD));
+
+	n += put_vlan(buf + n, 200, htons(ETH_P_IP));
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+/* IPv4 with a larger options block (IHL 8 => 12 bytes of options). */
+static int build_ipv4_options12_tcp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_TCP, 8, 0);
+
+	return n + put_ports(buf + n);
+}
+
+/* PPPoE with a PFC-compressed (odd MSB) PPP protocol field: defers. */
+static int build_pppoe_pfc(u8 *buf)
+{
+	int n = put_pppoe(buf, 0x2100, 20 + 4);
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+/* Mid-stream fragment: non-zero offset AND MF still set. */
+static int build_ipv4_frag_mid_tcp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_TCP, 5, htons(IP_MF | 5));
+
+	return n + put_ports(buf + n);
+}
+
+/* GRE optional-field combinations (order: csum+reserved, key, seq). */
+static int build_gre_csum_key_ipv4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	n += put_gre(buf + n, GRE_CSUM | GRE_KEY, htons(ETH_P_IP));
+	memset(buf + n, 0, 8);		/* checksum + reserved, key */
+	n += 8;
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_csum_key_seq_ipv4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	n += put_gre(buf + n, GRE_CSUM | GRE_KEY | GRE_SEQ, htons(ETH_P_IP));
+	memset(buf + n, 0, 12);		/* checksum + reserved, key, seq */
+	n += 12;
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+/* Priority-tagged frame (802.1Q VID 0, only PCP bits in the TCI). */
+static int build_vlan_prio_ipv4_tcp(u8 *buf)
+{
+	int n = put_vlan(buf, 5 << VLAN_PRIO_SHIFT, htons(ETH_P_IP));
+
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+/* IPv6 routing extension header before TCP. */
+static int build_ipv6_routing_tcp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_ROUTING, 0);
+
+	n += put_ext_hdr(buf + n, IPPROTO_TCP);
+	return n + put_ports(buf + n);
+}
+
+/* Deeply nested IPIP / GRE chains for the recursion-cap regression. */
+static int build_ipip_nest(u8 *buf, int levels)
+{
+	int n = 0, i;
+
+	for (i = 0; i < levels; i++)
+		n += put_ipv4(buf + n, IPPROTO_IPIP, 5, 0);
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_nest(u8 *buf, int levels)
+{
+	int n = 0, i;
+
+	for (i = 0; i < levels; i++) {
+		n += put_ipv4(buf + n, IPPROTO_GRE, 5, 0);
+		n += put_gre(buf + n, 0, htons(ETH_P_IP));
+	}
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_gre_csum_ipv4(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_GRE, 5, 0);
+
+	n += put_gre(buf + n, GRE_CSUM, htons(ETH_P_IP));
+	memset(buf + n, 0, 4);		/* checksum + reserved */
+	n += 4;
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv4_options_tcp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_TCP, 6, 0);	/* IHL=6: 4B options */
+
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv4_frag_tcp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_TCP, 5, htons(IP_MF));
+
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv4_frag_offset_tcp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_TCP, 5, htons(0x0010));
+
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv4_icmp(u8 *buf)
+{
+	int n = put_ipv4(buf, IPPROTO_ICMP, 5, 0);
+	u8 icmp[8] = { 0x08, 0x00, 0x00, 0x00, 0x12, 0x34, 0x00, 0x01 };
+
+	memcpy(buf + n, icmp, sizeof(icmp));
+	return n + sizeof(icmp);
+}
+
+static int build_ipv6_hopopts_tcp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_HOPOPTS, 0);
+	u8 hbh[8] = { IPPROTO_TCP, 0, 0, 0, 0, 0, 0, 0 };
+
+	memcpy(buf + n, hbh, sizeof(hbh));
+	n += sizeof(hbh);
+	return n + put_ports(buf + n);
+}
+
+static int build_ipv6_flowlabel_tcp(u8 *buf)
+{
+	int n = put_ipv6(buf, IPPROTO_TCP, 0xbeef);
+
+	return n + put_ports(buf + n);
+}
+
+struct fd_fast_case {
+	const char *name;
+	int (*build)(u8 *buf);
+	__be16 proto;
+	unsigned int flags;
+};
+
+/* htons() is not constant-foldable here without cpu_to_be16 literals;
+ * use cpu_to_be16 for static initializers.
+ */
+static const struct fd_fast_case fd_fast_cases[] = {
+	/* eligible shapes: the fast path should take these */
+	{ "ipv4_tcp", build_ipv4_tcp, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv4_udp", build_ipv4_udp, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv6_tcp", build_ipv6_tcp, cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "ipv6_udp", build_ipv6_udp, cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "vlan_ipv4_tcp", build_vlan_ipv4_tcp, cpu_to_be16(ETH_P_8021Q), 0 },
+	{ "vlan_ipv6_udp", build_vlan_ipv6_udp, cpu_to_be16(ETH_P_8021Q), 0 },
+	{ "qinq_ipv4_tcp", build_qinq_ipv4_tcp, cpu_to_be16(ETH_P_8021AD), 0 },
+	{ "pppoe_ipv4_tcp", build_pppoe_ipv4_tcp, cpu_to_be16(ETH_P_PPP_SES), 0 },
+	{ "pppoe_ipv6_tcp", build_pppoe_ipv6_tcp, cpu_to_be16(ETH_P_PPP_SES), 0 },
+	{ "mpls_ipv4", build_mpls_ipv4, cpu_to_be16(ETH_P_MPLS_UC), 0 },
+	{ "ipip", build_ipip, cpu_to_be16(ETH_P_IP), 0 },
+	{ "6in4", build_6in4, cpu_to_be16(ETH_P_IP), 0 },
+	{ "4in6", build_4in6, cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "4in6_flowlabel", build_4in6_flowlabel, cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "6in6", build_6in6, cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "gre_ipv4", build_gre_ipv4, cpu_to_be16(ETH_P_IP), 0 },
+	{ "gre6_ipv4", build_gre6_ipv4, cpu_to_be16(ETH_P_IPV6), 0 },
+	/* deliberate fast-path misses: must fall back with identical
+	 * output (and, per the zeroed containers, no partial writes)
+	 */
+	{ "ipv4_options_tcp", build_ipv4_options_tcp, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv4_frag_mf", build_ipv4_frag_tcp, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv4_frag_mf_1stfrag", build_ipv4_frag_tcp, cpu_to_be16(ETH_P_IP),
+	  FLOW_DISSECTOR_F_PARSE_1ST_FRAG },
+	{ "ipv4_frag_offset", build_ipv4_frag_offset_tcp,
+	  cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv4_icmp", build_ipv4_icmp, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv6_hopopts_tcp", build_ipv6_hopopts_tcp,
+	  cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "ipv6_flowlabel_tcp", build_ipv6_flowlabel_tcp,
+	  cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "vlan3_ipv4_tcp", build_vlan3_ipv4_tcp, cpu_to_be16(ETH_P_8021Q), 0 },
+	{ "pppoe_lcp", build_pppoe_lcp, cpu_to_be16(ETH_P_PPP_SES), 0 },
+	{ "mpls_multi_label", build_mpls_multi, cpu_to_be16(ETH_P_MPLS_UC), 0 },
+	{ "gre_csum_ipv4", build_gre_csum_ipv4, cpu_to_be16(ETH_P_IP), 0 },
+	/* corner cases */
+	{ "gre_seq_ipv4", build_gre_seq_ipv4, cpu_to_be16(ETH_P_IP), 0 },
+	{ "gre_key_ipv4", build_gre_key_ipv4, cpu_to_be16(ETH_P_IP), 0 },
+	{ "gre_version1", build_gre_version1, cpu_to_be16(ETH_P_IP), 0 },
+	{ "gre_teb", build_gre_teb, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv6_hopopts_dest_tcp", build_ipv6_hopopts_dest_tcp,
+	  cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "ipv6_frag_tcp", build_ipv6_frag_tcp, cpu_to_be16(ETH_P_IPV6), 0 },
+	{ "vlan_q_then_ad_ipv4", build_vlan_q_then_ad_ipv4,
+	  cpu_to_be16(ETH_P_8021Q), 0 },
+	{ "ipv4_options12_tcp", build_ipv4_options12_tcp,
+	  cpu_to_be16(ETH_P_IP), 0 },
+	{ "pppoe_pfc", build_pppoe_pfc, cpu_to_be16(ETH_P_PPP_SES), 0 },
+	{ "ipv4_frag_mid", build_ipv4_frag_mid_tcp, cpu_to_be16(ETH_P_IP), 0 },
+	{ "ipv4_frag_mid_1stfrag", build_ipv4_frag_mid_tcp,
+	  cpu_to_be16(ETH_P_IP), FLOW_DISSECTOR_F_PARSE_1ST_FRAG },
+	{ "gre_csum_key_ipv4", build_gre_csum_key_ipv4,
+	  cpu_to_be16(ETH_P_IP), 0 },
+	{ "gre_csum_key_seq_ipv4", build_gre_csum_key_seq_ipv4,
+	  cpu_to_be16(ETH_P_IP), 0 },
+	{ "vlan_prio_ipv4_tcp", build_vlan_prio_ipv4_tcp,
+	  cpu_to_be16(ETH_P_8021Q), 0 },
+	{ "ipv6_routing_tcp", build_ipv6_routing_tcp,
+	  cpu_to_be16(ETH_P_IPV6), 0 },
+};
+
+static void fd_fast_case_to_desc(const struct fd_fast_case *c, char *desc)
+{
+	strscpy(desc, c->name, KUNIT_PARAM_DESC_SIZE);
+}
+
+KUNIT_ARRAY_PARAM(fd_fast, fd_fast_cases, fd_fast_case_to_desc);
+
+/* Dissect the same bytes into a given dissector with all gates off, then
+ * all gates on; the return values and every byte of the output containers
+ * must match. Used both for the eligible dissectors (equivalence) and for
+ * an ineligible one (which the dispatcher must reject, so on==off trivially).
+ */
+static void fd_check_one(struct kunit *test, struct flow_dissector *fd,
+			 const u8 *data, __be16 proto, int hlen,
+			 unsigned int flags)
+{
+	struct flow_keys keys_slow, keys_fast;
+	bool ret_slow, ret_fast;
+
+	fd_fast_gates_set(false);
+	memset(&keys_slow, 0, sizeof(keys_slow));
+	ret_slow = __skb_flow_dissect(&init_net, NULL, fd, &keys_slow,
+				      data, proto, 0, hlen, flags);
+
+	fd_fast_gates_set(true);
+	memset(&keys_fast, 0, sizeof(keys_fast));
+	ret_fast = __skb_flow_dissect(&init_net, NULL, fd, &keys_fast,
+				      data, proto, 0, hlen, flags);
+	fd_fast_gates_set(false);
+
+	KUNIT_EXPECT_EQ(test, ret_slow, ret_fast);
+	KUNIT_EXPECT_MEMEQ(test, &keys_slow, &keys_fast, sizeof(keys_slow));
+	/* flow_keys -> skb->hash is what consumers see; assert it too. */
+	KUNIT_EXPECT_EQ(test, flow_hash_from_keys(&keys_slow),
+			flow_hash_from_keys(&keys_fast));
+}
+
+/* Check against both eligible dissectors (flow_keys_dissector and the
+ * symmetric one — different used_keys, so different fast-path branches).
+ */
+static void fd_fast_check_equiv(struct kunit *test, const u8 *data,
+				__be16 proto, int hlen, unsigned int flags)
+{
+	/* skb_get_hash() -- RPS/RFS, fq, cake -- passes
+	 * FLOW_DISSECTOR_F_STOP_AT_FLOW_LABEL on every dissect, so each
+	 * case must hold with and without it, against both dissectors.
+	 */
+	unsigned int fl = flags | FLOW_DISSECTOR_F_STOP_AT_FLOW_LABEL;
+
+	fd_check_one(test, &flow_keys_dissector, data, proto, hlen, flags);
+	fd_check_one(test, flow_keys_dissector_symmetric_kunit(),
+		     data, proto, hlen, flags);
+	fd_check_one(test, &flow_keys_dissector, data, proto, hlen, fl);
+	fd_check_one(test, flow_keys_dissector_symmetric_kunit(),
+		     data, proto, hlen, fl);
+}
+
+static void fd_fast_equiv_test(struct kunit *test)
+{
+	const struct fd_fast_case *c = test->param_value;
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	len = c->build(buf);
+	KUNIT_ASSERT_LE(test, len, FD_TEST_BUF_LEN);
+
+	fd_fast_check_equiv(test, buf, c->proto, len, c->flags);
+}
+
+/* Truncate every corpus packet at every byte boundary: the fast path
+ * must never parse (or partially write) anything the slow path would
+ * have rejected for the same length.
+ */
+static void fd_fast_truncation_test(struct kunit *test)
+{
+	int i, len, hlen;
+	u8 *buf;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	for (i = 0; i < ARRAY_SIZE(fd_fast_cases); i++) {
+		const struct fd_fast_case *c = &fd_fast_cases[i];
+
+		len = c->build(buf);
+		for (hlen = len - 1; hlen >= 0; hlen--)
+			fd_fast_check_equiv(test, buf, c->proto, hlen,
+					    c->flags);
+	}
+}
+
+/* skb-mode entry conditions the raw-data mode can't reach: the
+ * hardware-stripped outer VLAN tag (skb_vlan_tag_present()).
+ */
+static struct sk_buff *fd_fast_build_skb(struct kunit *test,
+					 int (*build)(u8 *buf),
+					 __be16 protocol)
+{
+	struct sk_buff *skb;
+	int len;
+	u8 *buf;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+	len = build(buf);
+
+	skb = alloc_skb(FD_TEST_BUF_LEN + NET_SKB_PAD, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, skb);
+	skb_reserve(skb, NET_SKB_PAD);
+	skb_put_data(skb, buf, len);
+	skb->protocol = protocol;
+	skb_reset_mac_header(skb);
+	skb_reset_network_header(skb);
+	return skb;
+}
+
+static void fd_check_one_skb(struct kunit *test, struct flow_dissector *fd,
+			     struct sk_buff *skb)
+{
+	struct flow_keys keys_slow, keys_fast;
+	bool ret_slow, ret_fast;
+
+	fd_fast_gates_set(false);
+	memset(&keys_slow, 0, sizeof(keys_slow));
+	ret_slow = __skb_flow_dissect(&init_net, skb, fd,
+				      &keys_slow, NULL, 0, 0, 0, 0);
+
+	fd_fast_gates_set(true);
+	memset(&keys_fast, 0, sizeof(keys_fast));
+	ret_fast = __skb_flow_dissect(&init_net, skb, fd,
+				      &keys_fast, NULL, 0, 0, 0, 0);
+	fd_fast_gates_set(false);
+
+	KUNIT_EXPECT_EQ(test, ret_slow, ret_fast);
+	KUNIT_EXPECT_MEMEQ(test, &keys_slow, &keys_fast, sizeof(keys_slow));
+	KUNIT_EXPECT_EQ(test, flow_hash_from_keys(&keys_slow),
+			flow_hash_from_keys(&keys_fast));
+}
+
+static void fd_fast_check_equiv_skb(struct kunit *test, struct sk_buff *skb)
+{
+	fd_check_one_skb(test, &flow_keys_dissector, skb);
+	fd_check_one_skb(test, flow_keys_dissector_symmetric_kunit(), skb);
+}
+
+static void fd_fast_skb_plain_test(struct kunit *test)
+{
+	struct sk_buff *skb;
+
+	skb = fd_fast_build_skb(test, build_ipv4_tcp, htons(ETH_P_IP));
+	fd_fast_check_equiv_skb(test, skb);
+	kfree_skb(skb);
+}
+
+static void fd_fast_skb_hwaccel_vlan_test(struct kunit *test)
+{
+	struct sk_buff *skb;
+
+	/* Payload is plain IPv4+TCP; the VLAN tag lives in skb metadata
+	 * as after hardware tag stripping.
+	 */
+	skb = fd_fast_build_skb(test, build_ipv4_tcp, htons(ETH_P_IP));
+	__vlan_hwaccel_put_tag(skb, htons(ETH_P_8021Q), 100);
+	fd_fast_check_equiv_skb(test, skb);
+	kfree_skb(skb);
+}
+
+static void fd_fast_skb_hwaccel_vlan_qinq_test(struct kunit *test)
+{
+	struct sk_buff *skb;
+
+	/* Outer tag hw-stripped (8021AD), second tag in the payload. */
+	skb = fd_fast_build_skb(test, build_vlan_ipv4_tcp, htons(ETH_P_8021Q));
+	__vlan_hwaccel_put_tag(skb, htons(ETH_P_8021AD), 100);
+	fd_fast_check_equiv_skb(test, skb);
+	kfree_skb(skb);
+}
+
+/* Deeply nested IPIP / GRE tunnels: the fast path must mirror the slow
+ * path's MAX_FLOW_DISSECT_HDRS bound, not recurse without limit. For
+ * shallow stacks both descend and agree; past the cap the fast path
+ * defers and equals the slow path's (capped) result. Depths stay well
+ * below anything that could exhaust the test kernel's stack.
+ */
+static void fd_fast_deep_nest_test(struct kunit *test)
+{
+	int levels, len;
+	u8 *buf;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	for (levels = 1; levels <= 24; levels++) {
+		len = build_ipip_nest(buf, levels);
+		if (len > FD_TEST_BUF_LEN)
+			break;
+		fd_fast_check_equiv(test, buf, htons(ETH_P_IP), len, 0);
+	}
+	for (levels = 1; levels <= 16; levels++) {
+		len = build_gre_nest(buf, levels);
+		if (len > FD_TEST_BUF_LEN)
+			break;
+		fd_fast_check_equiv(test, buf, htons(ETH_P_IP), len, 0);
+	}
+}
+
+/* Non-linear skb: the header spills past skb_headlen() into a page frag.
+ * The fast path reads only the linear head, so it must defer; the slow
+ * path pulls the header out of the frag via __skb_header_pointer. Output
+ * must match either way.
+ */
+static void fd_fast_nonlinear_skb_test(struct kunit *test)
+{
+	const int linear = 8;		/* less than an IPv4 header */
+	struct sk_buff *skb;
+	struct page *page;
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+	len = build_ipv4_tcp(buf);
+
+	skb = alloc_skb(NET_SKB_PAD + linear, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, skb);
+	skb_reserve(skb, NET_SKB_PAD);
+	skb_put_data(skb, buf, linear);
+
+	page = alloc_page(GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, page);
+	memcpy(page_address(page), buf + linear, len - linear);
+	skb_add_rx_frag(skb, 0, page, 0, len - linear, len - linear);
+
+	skb->protocol = htons(ETH_P_IP);
+	skb_reset_mac_header(skb);
+	skb_reset_network_header(skb);
+
+	fd_fast_check_equiv_skb(test, skb);
+	kfree_skb(skb);			/* releases the frag page */
+}
+
+/* A dissector that is neither flow_keys_dissector nor the symmetric one
+ * must be rejected by the fast-path eligibility gate — so gates-on output
+ * equals gates-off output for every corpus packet.
+ */
+static void fd_fast_ineligible_dissector_test(struct kunit *test)
+{
+	static const struct flow_dissector_key keys[] = {
+		{ .key_id = FLOW_DISSECTOR_KEY_CONTROL,
+		  .offset = offsetof(struct flow_keys, control) },
+		{ .key_id = FLOW_DISSECTOR_KEY_BASIC,
+		  .offset = offsetof(struct flow_keys, basic) },
+		{ .key_id = FLOW_DISSECTOR_KEY_IPV4_ADDRS,
+		  .offset = offsetof(struct flow_keys, addrs.v4addrs) },
+		{ .key_id = FLOW_DISSECTOR_KEY_IPV6_ADDRS,
+		  .offset = offsetof(struct flow_keys, addrs.v6addrs) },
+		{ .key_id = FLOW_DISSECTOR_KEY_PORTS,
+		  .offset = offsetof(struct flow_keys, ports) },
+		{ .key_id = FLOW_DISSECTOR_KEY_ICMP,
+		  .offset = offsetof(struct flow_keys, icmp) },
+	};
+	struct flow_dissector custom;
+	int i, len;
+	u8 *buf;
+
+	skb_flow_dissector_init(&custom, keys, ARRAY_SIZE(keys));
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	for (i = 0; i < ARRAY_SIZE(fd_fast_cases); i++) {
+		const struct fd_fast_case *c = &fd_fast_cases[i];
+
+		len = c->build(buf);
+		fd_check_one(test, &custom, buf, c->proto, len, c->flags);
+	}
+}
+
+/* skb-mode coverage for the shapes the original suite only exercised in
+ * raw-data mode (ipv6 / pppoe / mpls / gre).
+ */
+static void fd_fast_skb_shapes_test(struct kunit *test)
+{
+	static const struct {
+		int (*build)(u8 *buf);
+		__be16 proto;
+	} cases[] = {
+		{ build_ipv6_tcp, cpu_to_be16(ETH_P_IPV6) },
+		{ build_pppoe_ipv4_tcp, cpu_to_be16(ETH_P_PPP_SES) },
+		{ build_mpls_ipv4, cpu_to_be16(ETH_P_MPLS_UC) },
+		{ build_gre_ipv4, cpu_to_be16(ETH_P_IP) },
+	};
+	int i;
+
+	for (i = 0; i < ARRAY_SIZE(cases); i++) {
+		struct sk_buff *skb;
+
+		skb = fd_fast_build_skb(test, cases[i].build, cases[i].proto);
+		fd_fast_check_equiv_skb(test, skb);
+		kfree_skb(skb);
+	}
+}
+
+/* Seeded, reproducible fuzzer. Builds a real-ish packet from a random
+ * corpus shape, mutates header bytes and the length, and asserts
+ * fast == slow every iteration (over both dissectors). Fixed seed so a
+ * failure reproduces exactly. This is the general guard for divergences
+ * the hand-written corpus doesn't name.
+ */
+static void fd_fast_fuzz_test(struct kunit *test)
+{
+	struct rnd_state rnd;
+	u8 *buf;
+	int i;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+	prandom_seed_state(&rnd, 0x1a2b3c4d5e6f7788ULL);
+
+	for (i = 0; i < FD_FUZZ_ITERS; i++) {
+		const struct fd_fast_case *c;
+		unsigned int flags;
+		int len, j, nmut;
+
+		c = &fd_fast_cases[prandom_u32_state(&rnd) %
+				   ARRAY_SIZE(fd_fast_cases)];
+		len = c->build(buf);
+
+		/* Mutate a handful of header bytes to random values. */
+		nmut = prandom_u32_state(&rnd) % (len + 1);
+		for (j = 0; j < nmut; j++)
+			buf[prandom_u32_state(&rnd) % len] =
+				prandom_u32_state(&rnd);
+
+		/* Random truncation and random 1st-frag flag. */
+		len = prandom_u32_state(&rnd) % (len + 1);
+		flags = (prandom_u32_state(&rnd) & 1) ?
+			FLOW_DISSECTOR_F_PARSE_1ST_FRAG : 0;
+
+		fd_fast_check_equiv(test, buf, c->proto, len, flags);
+	}
+}
+
+/* --- FOU/GUE inner-descent tests ---
+ *
+ * FOU/GUE have no well-known UDP port, so the descent resolves the port
+ * through the fou module's registered ops. The test registers a mock
+ * ops (so no real fou config / socket is needed) and dissects in skb
+ * mode with a dummy netdev (the lookup derives the netns from the skb).
+ * Unlike the byte-identical tests, descent changes the output, so these
+ * assert the *inner* 5-tuple is what came out.
+ */
+static __be16 fd_mock_port;
+static u16 fd_mock_type;
+static u8 fd_mock_proto;
+
+static u16 fd_mock_encap_lookup(const struct net *net, u8 family,
+				__be16 port, u8 *protocol)
+{
+	if (port != fd_mock_port)
+		return FOU_ENCAP_UNSPEC;
+	*protocol = fd_mock_proto;
+	return fd_mock_type;
+}
+
+static const struct flow_dissector_fou_ops fd_mock_fou_ops = {
+	.encap_lookup = fd_mock_encap_lookup,
+};
+
+static int put_udp(u8 *buf, __be16 dport)
+{
+	struct udphdr *uh = (struct udphdr *)buf;
+
+	uh->source = htons(0x9999);
+	uh->dest = dport;
+	uh->len = 0;
+	uh->check = 0;
+	return sizeof(*uh);
+}
+
+static int put_gue(u8 *buf, u8 proto_ctype)
+{
+	struct guehdr *gh = (struct guehdr *)buf;
+
+	gh->word = 0;
+	gh->proto_ctype = proto_ctype;	/* version 0, control 0, hlen 0 */
+	return sizeof(*gh);
+}
+
+/* Build outer IPv4+UDP(dport) + optional GUE shim + inner IPv4+TCP. The
+ * outer IP uses distinct addresses (10.0.0.x) from the inner (192.168.x,
+ * from put_ipv4) so the test can tell inner-descent from the outer parse.
+ */
+static int build_fou_gue_pkt(u8 *buf, __be16 dport, bool gue)
+{
+	struct iphdr *outer = (struct iphdr *)buf;
+	int n = put_ipv4(buf, IPPROTO_UDP, 5, 0);
+
+	outer->saddr = htonl(0x0a000001);	/* 10.0.0.1 */
+	outer->daddr = htonl(0x0a000002);	/* 10.0.0.2 */
+	n += put_udp(buf + n, dport);
+	if (gue)
+		n += put_gue(buf + n, IPPROTO_IPIP);
+	n += put_ipv4(buf + n, IPPROTO_TCP, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static void fd_fou_gue_descend(struct kunit *test, bool gue,
+			       struct static_key_false *gate)
+{
+	struct net_device *dev;
+	struct flow_keys keys;
+	struct sk_buff *skb;
+	__be16 dport = htons(0x17c0);
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+	len = build_fou_gue_pkt(buf, dport, gue);
+
+	dev = alloc_netdev(0, "fdtest%d", NET_NAME_UNKNOWN, ether_setup);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, dev);
+
+	skb = alloc_skb(FD_TEST_BUF_LEN + NET_SKB_PAD, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, skb);
+	skb_reserve(skb, NET_SKB_PAD);
+	skb_put_data(skb, buf, len);
+	skb->protocol = htons(ETH_P_IP);
+	skb->dev = dev;
+	skb_reset_network_header(skb);
+
+	fd_mock_port = dport;
+	fd_mock_type = gue ? FOU_ENCAP_GUE : FOU_ENCAP_DIRECT;
+	fd_mock_proto = IPPROTO_IPIP;
+	KUNIT_ASSERT_EQ(test, 0,
+			flow_dissector_fou_ops_register(&fd_mock_fou_ops));
+
+	/* The descent lives inside the eth_ip fast path, so that gate must
+	 * be on too for the dispatcher to reach it.
+	 */
+	static_branch_enable(&flow_dissector_eth_ip_key);
+	static_branch_enable(gate);
+	memset(&keys, 0, sizeof(keys));
+	__skb_flow_dissect(&init_net, skb, &flow_keys_dissector, &keys,
+			   NULL, 0, 0, 0, 0);
+	static_branch_disable(gate);
+	static_branch_disable(&flow_dissector_eth_ip_key);
+	flow_dissector_fou_ops_unregister(&fd_mock_fou_ops);
+
+	/* Descent must have reached the inner IPv4 header: ENCAP stamped,
+	 * inner v4 addrs (192.168.1.1 -> 192.168.2.2 from put_ipv4), inner
+	 * ports (0x1234/0x5678 from put_ports) — i.e. the inner 5-tuple,
+	 * not the outer UDP one.
+	 */
+	KUNIT_EXPECT_TRUE(test, keys.control.flags & FLOW_DIS_ENCAPSULATION);
+	KUNIT_EXPECT_EQ(test, keys.basic.ip_proto, IPPROTO_TCP);
+	KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.src), 0xc0a80101);
+	KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.dst), 0xc0a80202);
+	KUNIT_EXPECT_EQ(test, ntohs(keys.ports.src), 0x1234);
+	KUNIT_EXPECT_EQ(test, ntohs(keys.ports.dst), 0x5678);
+
+	kfree_skb(skb);
+	free_netdev(dev);
+}
+
+static void fd_fast_gue_descend_test(struct kunit *test)
+{
+	fd_fou_gue_descend(test, true, &flow_dissector_gue_inner_key);
+}
+
+static void fd_fast_fou_descend_test(struct kunit *test)
+{
+	fd_fou_gue_descend(test, false, &flow_dissector_fou_inner_key);
+}
+
+/* ---- UDP-tunnel descent fast/slow equivalence ------------------------
+ *
+ * Once the slow path descends into UDP tunnels too (gated, off by
+ * default), the descents are byte-identical like every other shape: with
+ * a descent gate pinned on, the fast path (entry gates on) and the slow
+ * path (entry gates off, so the fast dispatcher defers) must descend into
+ * the same inner flow and produce identical flow_keys. These use skb mode
+ * (the FOU/GUE port lookup derives the netns from the skb; VXLAN/Geneve/
+ * GTP-U work in skb mode too) with a dummy netdev.
+ */
+
+#define FD_FOU_DPORT	htons(0x17c0)
+
+static int put_vxlanhdr(u8 *buf, __be32 flags)
+{
+	struct vxlanhdr *vh = (struct vxlanhdr *)buf;
+
+	vh->vx_flags = flags;
+	vh->vx_vni = htonl(0x123400);
+	return sizeof(*vh);
+}
+
+static int put_genevehdr(u8 *buf, __be16 proto_type, u8 opt_len)
+{
+	struct genevehdr *gnv = (struct genevehdr *)buf;
+
+	memset(gnv, 0, sizeof(*gnv));		/* ver = 0 */
+	gnv->opt_len = opt_len;
+	gnv->proto_type = proto_type;
+	return sizeof(*gnv) + opt_len * 4;
+}
+
+static int put_gtp1(u8 *buf, u8 type)
+{
+	struct gtp1_header *gtp = (struct gtp1_header *)buf;
+
+	memset(gtp, 0, sizeof(*gtp));
+	gtp->flags = 0x30;			/* version 1, PT 1, no options */
+	gtp->type = type;			/* 0xff = G-PDU */
+	return sizeof(*gtp);
+}
+
+static int put_inner_eth(u8 *buf, __be16 proto)
+{
+	struct ethhdr *eth = (struct ethhdr *)buf;
+
+	memset(eth, 0, sizeof(*eth));
+	eth->h_proto = proto;
+	return ETH_HLEN;
+}
+
+static int put_outer_ipv4_udp(u8 *buf, __be16 dport)
+{
+	struct iphdr *o = (struct iphdr *)buf;
+	int n = put_ipv4(buf, IPPROTO_UDP, 5, 0);
+
+	o->saddr = htonl(0x0a000001);		/* 10.0.0.1 (distinct outer) */
+	o->daddr = htonl(0x0a000002);		/* 10.0.0.2 */
+	return n + put_udp(buf + n, dport);
+}
+
+static int put_outer_ipv6_udp(u8 *buf, __be16 dport)
+{
+	struct ipv6hdr *o = (struct ipv6hdr *)buf;
+	int n = put_ipv6(buf, IPPROTO_UDP, 0);
+
+	o->saddr.s6_addr32[1] = htonl(0x0a0a0a0a);	/* distinct outer */
+	o->daddr.s6_addr32[1] = htonl(0x0b0b0b0b);
+	return n + put_udp(buf + n, dport);
+}
+
+static int put_inner_ip(u8 *buf, bool inner_v6, u8 inner_l4)
+{
+	int n;
+
+	if (inner_v6)
+		n = put_ipv6(buf, inner_l4, 0);
+	else
+		n = put_ipv4(buf, inner_l4, 5, 0);
+	return n + put_ports(buf + n);
+}
+
+static int build_vxlan(u8 *buf, bool inner_v6, u8 inner_l4)
+{
+	int n = put_outer_ipv4_udp(buf, htons(IANA_VXLAN_UDP_PORT));
+
+	n += put_vxlanhdr(buf + n, VXLAN_HF_VNI);
+	n += put_inner_eth(buf + n, inner_v6 ? htons(ETH_P_IPV6)
+					     : htons(ETH_P_IP));
+	return n + put_inner_ip(buf + n, inner_v6, inner_l4);
+}
+
+/* VXLAN with non-conforming flags: I unset, or extension bits (GBP)
+ * set. RFC 7348 receivers must check I=1; the descent must not trust
+ * such a frame and keeps the outer keys.
+ */
+static int build_vxlan_flags(u8 *buf, __be32 flags)
+{
+	int n = put_outer_ipv4_udp(buf, htons(IANA_VXLAN_UDP_PORT));
+
+	n += put_vxlanhdr(buf + n, flags);
+	n += put_inner_eth(buf + n, htons(ETH_P_IP));
+	return n + put_inner_ip(buf + n, false, IPPROTO_TCP);
+}
+
+static int build_geneve(u8 *buf, bool inner_v6, u8 inner_l4, u8 opt_len)
+{
+	int n = put_outer_ipv4_udp(buf, htons(GENEVE_UDP_PORT));
+
+	n += put_genevehdr(buf + n, htons(ETH_P_TEB), opt_len);
+	n += put_inner_eth(buf + n, inner_v6 ? htons(ETH_P_IPV6)
+					     : htons(ETH_P_IP));
+	return n + put_inner_ip(buf + n, inner_v6, inner_l4);
+}
+
+/* Geneve OAM control frame (RFC 8926 §3.1): must not descend. */
+static int build_geneve_oam(u8 *buf)
+{
+	int n = put_outer_ipv4_udp(buf, htons(GENEVE_UDP_PORT));
+	struct genevehdr *gnv = (struct genevehdr *)(buf + n);
+
+	n += put_genevehdr(buf + n, htons(ETH_P_TEB), 0);
+	gnv->oam = 1;
+	n += put_inner_eth(buf + n, htons(ETH_P_IP));
+	return n + put_inner_ip(buf + n, false, IPPROTO_TCP);
+}
+
+static int build_gtpu(u8 *buf, bool inner_v6, u8 inner_l4, u8 gtp_type)
+{
+	int n = put_outer_ipv4_udp(buf, htons(GTP1U_PORT));
+
+	n += put_gtp1(buf + n, gtp_type);
+	return n + put_inner_ip(buf + n, inner_v6, inner_l4);
+}
+
+/* GTP-U G-PDU with an option flag (S) set: the header grows a variable
+ * tail, so the descent must not trust the fixed-size layout.
+ */
+static int build_gtpu_espn(u8 *buf)
+{
+	int n = put_outer_ipv4_udp(buf, htons(GTP1U_PORT));
+	struct gtp1_header *gtp = (struct gtp1_header *)(buf + n);
+
+	n += put_gtp1(buf + n, 0xff);
+	gtp->flags = 0x32;		/* version 1, PT 1, S bit */
+	return n + put_inner_ip(buf + n, false, IPPROTO_TCP);
+}
+
+static int build_fou_gue(u8 *buf, bool gue, bool outer_v6, bool inner_v6,
+			 u8 inner_l4)
+{
+	int n = outer_v6 ? put_outer_ipv6_udp(buf, FD_FOU_DPORT)
+			 : put_outer_ipv4_udp(buf, FD_FOU_DPORT);
+
+	if (gue)
+		n += put_gue(buf + n, inner_v6 ? IPPROTO_IPV6 : IPPROTO_IPIP);
+	return n + put_inner_ip(buf + n, inner_v6, inner_l4);
+}
+
+static int build_vxlan_nest(u8 *buf, int levels)
+{
+	int n = 0, i;
+
+	for (i = 0; i < levels; i++) {
+		n += put_outer_ipv4_udp(buf + n, htons(IANA_VXLAN_UDP_PORT));
+		n += put_vxlanhdr(buf + n, VXLAN_HF_VNI);
+		n += put_inner_eth(buf + n, htons(ETH_P_IP));
+	}
+	return n + put_inner_ip(buf + n, false, IPPROTO_TCP);
+}
+
+/* Descent gate pinned on; entry gates off (slow descends) vs on (fast
+ * descends). The two must be byte-identical.
+ */
+static void fd_descent_check(struct kunit *test, struct sk_buff *skb,
+			     struct static_key_false *descent_gate)
+{
+	struct flow_keys keys_slow, keys_fast;
+	bool ret_slow, ret_fast;
+
+	static_branch_enable(descent_gate);
+
+	fd_fast_gates_set(false);
+	memset(&keys_slow, 0, sizeof(keys_slow));
+	ret_slow = __skb_flow_dissect(&init_net, skb, &flow_keys_dissector,
+				      &keys_slow, NULL, 0, 0, 0, 0);
+
+	fd_fast_gates_set(true);
+	memset(&keys_fast, 0, sizeof(keys_fast));
+	ret_fast = __skb_flow_dissect(&init_net, skb, &flow_keys_dissector,
+				      &keys_fast, NULL, 0, 0, 0, 0);
+	fd_fast_gates_set(false);
+
+	static_branch_disable(descent_gate);
+
+	KUNIT_EXPECT_EQ(test, ret_slow, ret_fast);
+	KUNIT_EXPECT_MEMEQ(test, &keys_slow, &keys_fast, sizeof(keys_slow));
+	KUNIT_EXPECT_EQ(test, flow_hash_from_keys(&keys_slow),
+			flow_hash_from_keys(&keys_fast));
+}
+
+static struct sk_buff *fd_descent_skb(struct kunit *test, const u8 *buf,
+				      int len, __be16 outer_proto,
+				      struct net_device *dev)
+{
+	struct sk_buff *skb = alloc_skb(len + NET_SKB_PAD, GFP_KERNEL);
+
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, skb);
+	skb_reserve(skb, NET_SKB_PAD);
+	skb_put_data(skb, buf, len);
+	skb->protocol = outer_proto;
+	skb->dev = dev;
+	skb_reset_network_header(skb);
+	return skb;
+}
+
+static void fd_descent_run(struct kunit *test, const u8 *buf, int len,
+			   __be16 outer_proto, struct static_key_false *gate)
+{
+	struct net_device *dev;
+	struct sk_buff *skb;
+
+	dev = alloc_netdev(0, "fdtest%d", NET_NAME_UNKNOWN, ether_setup);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, dev);
+	skb = fd_descent_skb(test, buf, len, outer_proto, dev);
+	fd_descent_check(test, skb, gate);
+	kfree_skb(skb);
+	free_netdev(dev);
+}
+
+/* Assert the fast path actually reached the inner 5-tuple (guards against
+ * fast and slow being wrong in the same way, which equivalence can't see).
+ */
+static void fd_descent_assert_inner(struct kunit *test, const u8 *buf, int len,
+				    __be16 outer_proto,
+				    struct static_key_false *gate, bool inner_v6)
+{
+	struct net_device *dev;
+	struct flow_keys keys;
+	struct sk_buff *skb;
+
+	dev = alloc_netdev(0, "fdtest%d", NET_NAME_UNKNOWN, ether_setup);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, dev);
+	skb = fd_descent_skb(test, buf, len, outer_proto, dev);
+
+	static_branch_enable(&flow_dissector_eth_ip_key);
+	static_branch_enable(gate);
+	memset(&keys, 0, sizeof(keys));
+	__skb_flow_dissect(&init_net, skb, &flow_keys_dissector, &keys,
+			   NULL, 0, 0, 0, 0);
+	static_branch_disable(gate);
+	static_branch_disable(&flow_dissector_eth_ip_key);
+
+	KUNIT_EXPECT_TRUE(test, keys.control.flags & FLOW_DIS_ENCAPSULATION);
+	KUNIT_EXPECT_EQ(test, keys.basic.ip_proto, IPPROTO_TCP);
+	if (inner_v6) {
+		KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v6addrs.src.s6_addr32[3]), 1);
+		KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v6addrs.dst.s6_addr32[3]), 2);
+	} else {
+		KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.src), 0xc0a80101);
+		KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.dst), 0xc0a80202);
+	}
+	KUNIT_EXPECT_EQ(test, ntohs(keys.ports.src), 0x1234);
+	KUNIT_EXPECT_EQ(test, ntohs(keys.ports.dst), 0x5678);
+
+	kfree_skb(skb);
+	free_netdev(dev);
+}
+
+/* Assert the dissect kept the OUTER tuple — i.e. no descent happened
+ * (the frame failed the tunnel-header validation with the gate on).
+ */
+static void fd_descent_assert_outer(struct kunit *test, const u8 *buf, int len,
+				    struct static_key_false *gate, __be16 dport)
+{
+	struct net_device *dev;
+	struct flow_keys keys;
+	struct sk_buff *skb;
+
+	dev = alloc_netdev(0, "fdtest%d", NET_NAME_UNKNOWN, ether_setup);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, dev);
+	skb = fd_descent_skb(test, buf, len, htons(ETH_P_IP), dev);
+
+	static_branch_enable(&flow_dissector_eth_ip_key);
+	static_branch_enable(gate);
+	memset(&keys, 0, sizeof(keys));
+	__skb_flow_dissect(&init_net, skb, &flow_keys_dissector, &keys,
+			   NULL, 0, 0, 0, 0);
+	static_branch_disable(gate);
+	static_branch_disable(&flow_dissector_eth_ip_key);
+
+	KUNIT_EXPECT_FALSE(test, keys.control.flags & FLOW_DIS_ENCAPSULATION);
+	KUNIT_EXPECT_EQ(test, keys.basic.ip_proto, IPPROTO_UDP);
+	KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.src), 0x0a000001);
+	KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.dst), 0x0a000002);
+	KUNIT_EXPECT_EQ(test, ntohs(keys.ports.src), 0x9999);
+	KUNIT_EXPECT_EQ(test, ntohs(keys.ports.dst), ntohs(dport));
+
+	kfree_skb(skb);
+	free_netdev(dev);
+}
+
+static void fd_descent_vxlan_test(struct kunit *test)
+{
+	struct static_key_false *g = &flow_dissector_vxlan_inner_key;
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	len = build_vxlan(buf, false, IPPROTO_TCP);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_inner(test, buf, len, htons(ETH_P_IP), g, false);
+
+	len = build_vxlan(buf, false, IPPROTO_UDP);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+
+	len = build_vxlan(buf, true, IPPROTO_TCP);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_inner(test, buf, len, htons(ETH_P_IP), g, true);
+
+	/* inner ICMP: fast defers (inner not TCP/UDP) -> both run slow. */
+	len = build_vxlan(buf, false, IPPROTO_ICMP);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+
+	/* I flag unset: not valid RFC 7348 VXLAN — no descent, outer
+	 * tuple kept, fast == slow.
+	 */
+	len = build_vxlan_flags(buf, 0);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_outer(test, buf, len, g,
+				htons(IANA_VXLAN_UDP_PORT));
+
+	/* GBP extension bits set: outside plain VXLAN — same posture. */
+	len = build_vxlan_flags(buf, VXLAN_HF_GBP | VXLAN_HF_VNI);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_outer(test, buf, len, g,
+				htons(IANA_VXLAN_UDP_PORT));
+}
+
+static void fd_descent_geneve_test(struct kunit *test)
+{
+	struct static_key_false *g = &flow_dissector_geneve_inner_key;
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	len = build_geneve(buf, false, IPPROTO_TCP, 0);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_inner(test, buf, len, htons(ETH_P_IP), g, false);
+
+	len = build_geneve(buf, true, IPPROTO_UDP, 0);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+
+	/* opt_len != 0: outside the fast subset -> both keep outer keys. */
+	len = build_geneve(buf, false, IPPROTO_TCP, 1);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+
+	/* OAM control frame: never descend (RFC 8926 §3.1). */
+	len = build_geneve_oam(buf);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_outer(test, buf, len, g, htons(GENEVE_UDP_PORT));
+}
+
+static void fd_descent_gtpu_test(struct kunit *test)
+{
+	struct static_key_false *g = &flow_dissector_gtpu_inner_key;
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	len = build_gtpu(buf, false, IPPROTO_TCP, 0xff);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_inner(test, buf, len, htons(ETH_P_IP), g, false);
+
+	len = build_gtpu(buf, true, IPPROTO_TCP, 0xff);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_inner(test, buf, len, htons(ETH_P_IP), g, true);
+
+	/* not a G-PDU (type != 0xff): outside the fast subset. */
+	len = build_gtpu(buf, false, IPPROTO_TCP, 0xfe);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+
+	/* G-PDU with the S option flag: variable header length — outside
+	 * the subset, both paths keep the outer keys.
+	 */
+	len = build_gtpu_espn(buf);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP), g);
+	fd_descent_assert_outer(test, buf, len, g, htons(GTP1U_PORT));
+}
+
+static void fd_descent_fou_gue_test(struct kunit *test)
+{
+	static const struct { bool gue; bool outer_v6; bool inner_v6; } v[] = {
+		{ true,  false, false }, { true,  false, true  },
+		{ true,  true,  false }, { true,  true,  true  },
+		{ false, false, false }, { false, true,  false },
+	};
+	int i, len;
+	u8 *buf;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	for (i = 0; i < ARRAY_SIZE(v); i++) {
+		struct static_key_false *g = v[i].gue ?
+			&flow_dissector_gue_inner_key :
+			&flow_dissector_fou_inner_key;
+
+		len = build_fou_gue(buf, v[i].gue, v[i].outer_v6, v[i].inner_v6,
+				    IPPROTO_TCP);
+		fd_mock_port = FD_FOU_DPORT;
+		fd_mock_type = v[i].gue ? FOU_ENCAP_GUE : FOU_ENCAP_DIRECT;
+		fd_mock_proto = v[i].inner_v6 ? IPPROTO_IPV6 : IPPROTO_IPIP;
+		KUNIT_ASSERT_EQ(test, 0,
+				flow_dissector_fou_ops_register(&fd_mock_fou_ops));
+		fd_descent_run(test, buf, len,
+			       v[i].outer_v6 ? htons(ETH_P_IPV6) :
+					       htons(ETH_P_IP), g);
+		flow_dissector_fou_ops_unregister(&fd_mock_fou_ops);
+	}
+}
+
+/* Nested VXLAN past MAX_FLOW_DISSECT_HDRS: the fast path bails at the cap
+ * and defers to slow, the slow loop stops at the same depth -> identical.
+ */
+static void fd_descent_nest_test(struct kunit *test)
+{
+	const int cap = 1024;
+	u8 *buf;
+	int len;
+
+	buf = kunit_kzalloc(test, cap, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	len = build_vxlan_nest(buf, 16);
+	KUNIT_ASSERT_LE(test, len, cap);
+	fd_descent_run(test, buf, len, htons(ETH_P_IP),
+		       &flow_dissector_vxlan_inner_key);
+}
+
+/* Callers passing STOP_AT_ENCAP / STOP_BEFORE_ENCAP (RPS/RFS, tunnel
+ * drivers) rely on getting the OUTER tuple even when a descent gate is
+ * on: the slow-path descent entry is guarded on those flags, and the
+ * fast dispatcher rejects any flags beyond PARSE_1ST_FRAG. Assert both
+ * the outer result and fast/slow equivalence under the flags.
+ */
+static void fd_descent_stop_flags_test(struct kunit *test)
+{
+	static const unsigned int stop_flags[] = {
+		FLOW_DISSECTOR_F_STOP_AT_ENCAP,
+		FLOW_DISSECTOR_F_STOP_BEFORE_ENCAP,
+	};
+	struct flow_keys keys;
+	int i, len;
+	u8 *buf;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+	len = build_vxlan(buf, false, IPPROTO_TCP);
+
+	static_branch_enable(&flow_dissector_vxlan_inner_key);
+	for (i = 0; i < ARRAY_SIZE(stop_flags); i++) {
+		fd_check_one(test, &flow_keys_dissector, buf,
+			     htons(ETH_P_IP), len, stop_flags[i]);
+
+		static_branch_enable(&flow_dissector_eth_ip_key);
+		memset(&keys, 0, sizeof(keys));
+		__skb_flow_dissect(&init_net, NULL, &flow_keys_dissector,
+				   &keys, buf, htons(ETH_P_IP), 0, len,
+				   stop_flags[i]);
+		static_branch_disable(&flow_dissector_eth_ip_key);
+
+		KUNIT_EXPECT_FALSE(test,
+				   keys.control.flags & FLOW_DIS_ENCAPSULATION);
+		KUNIT_EXPECT_EQ(test, keys.basic.ip_proto, IPPROTO_UDP);
+		KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.src), 0x0a000001);
+		KUNIT_EXPECT_EQ(test, ntohl(keys.addrs.v4addrs.dst), 0x0a000002);
+		KUNIT_EXPECT_EQ(test, ntohs(keys.ports.dst), IANA_VXLAN_UDP_PORT);
+	}
+	static_branch_disable(&flow_dissector_vxlan_inner_key);
+}
+
+/* With every gate off the fast path must never run; the per-shape
+ * fast-hit counters are the observable. Positive control: with the
+ * gates on an eligible shape must count.
+ */
+static void fd_fast_gates_off_test(struct kunit *test)
+{
+	struct flow_keys keys;
+	u64 before;
+	int i, len;
+	u8 *buf;
+
+	buf = kunit_kzalloc(test, FD_TEST_BUF_LEN, GFP_KERNEL);
+	KUNIT_ASSERT_NOT_ERR_OR_NULL(test, buf);
+
+	fd_fast_gates_set(false);
+	before = flow_dissector_fast_hits_kunit();
+	for (i = 0; i < ARRAY_SIZE(fd_fast_cases); i++) {
+		len = fd_fast_cases[i].build(buf);
+		memset(&keys, 0, sizeof(keys));
+		__skb_flow_dissect(&init_net, NULL, &flow_keys_dissector,
+				   &keys, buf, fd_fast_cases[i].proto, 0,
+				   len, fd_fast_cases[i].flags);
+	}
+	KUNIT_EXPECT_EQ(test, flow_dissector_fast_hits_kunit(), before);
+
+	fd_fast_gates_set(true);
+	len = build_ipv4_tcp(buf);
+	memset(&keys, 0, sizeof(keys));
+	__skb_flow_dissect(&init_net, NULL, &flow_keys_dissector, &keys,
+			   buf, htons(ETH_P_IP), 0, len, 0);
+	KUNIT_EXPECT_GT(test, flow_dissector_fast_hits_kunit(), before);
+
+	/* The flagship consumer shape: skb_get_hash() (RPS/RFS, fq, cake)
+	 * passes STOP_AT_FLOW_LABEL -- the fast path must hit for it, or
+	 * it never runs on the kernel's standard receive path.
+	 */
+	before = flow_dissector_fast_hits_kunit();
+	memset(&keys, 0, sizeof(keys));
+	__skb_flow_dissect(&init_net, NULL, &flow_keys_dissector, &keys,
+			   buf, htons(ETH_P_IP), 0, len,
+			   FLOW_DISSECTOR_F_STOP_AT_FLOW_LABEL);
+	fd_fast_gates_set(false);
+	KUNIT_EXPECT_GT(test, flow_dissector_fast_hits_kunit(), before);
+}
+
+static void fd_all_gates_off(void)
+{
+	int i;
+
+	fd_fast_gates_set(false);
+	for (i = 0; i < ARRAY_SIZE(fd_descent_gates); i++)
+		static_branch_disable(fd_descent_gates[i]);
+}
+
+static int fd_fast_suite_init(struct kunit_suite *suite)
+{
+	fd_all_gates_off();
+	return 0;
+}
+
+static void fd_fast_suite_exit(struct kunit_suite *suite)
+{
+	fd_all_gates_off();
+}
+
+static struct kunit_case fd_fast_test_cases[] = {
+	KUNIT_CASE_PARAM(fd_fast_equiv_test, fd_fast_gen_params),
+	KUNIT_CASE(fd_fast_truncation_test),
+	KUNIT_CASE(fd_fast_deep_nest_test),
+	KUNIT_CASE(fd_fast_nonlinear_skb_test),
+	KUNIT_CASE(fd_fast_ineligible_dissector_test),
+	KUNIT_CASE(fd_fast_skb_plain_test),
+	KUNIT_CASE(fd_fast_skb_hwaccel_vlan_test),
+	KUNIT_CASE(fd_fast_skb_hwaccel_vlan_qinq_test),
+	KUNIT_CASE(fd_fast_skb_shapes_test),
+	KUNIT_CASE(fd_fast_fuzz_test),
+	KUNIT_CASE(fd_fast_gue_descend_test),
+	KUNIT_CASE(fd_fast_fou_descend_test),
+	KUNIT_CASE(fd_descent_vxlan_test),
+	KUNIT_CASE(fd_descent_geneve_test),
+	KUNIT_CASE(fd_descent_gtpu_test),
+	KUNIT_CASE(fd_descent_fou_gue_test),
+	KUNIT_CASE(fd_descent_nest_test),
+	KUNIT_CASE(fd_descent_stop_flags_test),
+	KUNIT_CASE(fd_fast_gates_off_test),
+	{}
+};
+
+static struct kunit_suite fd_fast_suite = {
+	.name = "flow_dissector_fastpath",
+	.suite_init = fd_fast_suite_init,
+	.suite_exit = fd_fast_suite_exit,
+	.test_cases = fd_fast_test_cases,
+};
+
+kunit_test_suite(fd_fast_suite);
+
+MODULE_DESCRIPTION("KUnit fast/slow equivalence tests for the flow dissector");
+MODULE_LICENSE("GPL");
-- 
2.54.0

